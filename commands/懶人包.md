---
description: 考前必背懶人包：把猜題範圍（或高頻考點）濃縮成必背條文重點＋記憶術一份文件
argument-hint: [可指定考點數量或科目，預設前 20 個考點]
---
使用 statute-memorizer skill 產出「考前必背懶人包」：依猜題範圍（exam-trend-forecast 之結果；無則依該等別加權頻率前 N 名）濃縮為考點→必背條文重點→修法新點→記憶術，可引用 `${CLAUDE_PLUGIN_ROOT}/reference/對照表/` 既有整理。完成後詢問是否存入 `<data_dir>/forecasts/`。範圍：$ARGUMENTS
