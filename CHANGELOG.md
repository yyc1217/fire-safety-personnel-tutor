# Changelog

本檔為本 plugin 的**唯一變更紀錄**：版本紀錄、資料與內容工作、測試計畫與驗收紀錄、決策紀錄與完成里程碑都在這裡。
尚未完成之待辦見 [`docs/待辦與優化建議.md`](docs/待辦與優化建議.md)。

> **沿革**：這些內容原先與待辦混雜於 `docs/待辦與優化建議.md`，先拆出為 `docs/變更紀錄.md`，
> 再於 2026-07-25 併入本檔（CHANGELOG.md 為官方慣例位置）。**兩次搬移均逐字保留內容**，只重組位置：
> 有對應版本者歸入「版本紀錄」，其餘資料與內容工作、決策紀錄、里程碑各自成節。

## 版本規則（重要）

`.claude-plugin/plugin.json` 的 `version` 是 Claude Code 判斷「使用者要不要更新」的**唯一依據**：
**只推 commit 不 bump 版本，已安裝的使用者收不到任何改變**，`/plugin update` 會回報「已是最新版本」。

因此：

> **凡異動 `skills/`、`reference/`、`corpus/`、`statutes/`，就必須 bump `plugin.json` 的 `version`（並同步 README 的 version badge）。**
> 這兩處是否一致由 CI（`scripts/ci_check_repo.py`）把關。

版號依 [semver](https://semver.org)：功能新增或行為改變 → MINOR；資料校正與文件修正 → PATCH。

---

# 版本紀錄

## [1.3.0] - 2026-08-03 — 出題檢索成本：四項實測驅動的規範修正（#73）

使用者回報 `/fs-quiz 緊急照明燈` 出題很慢，且體感是「卡住不動好幾分鐘」而非一直點權限同意。
**三次冷啟動實測**（子代理，各給與 Claude Code 相同的 `/fs-quiz` prompt，跑到首題呈現為止）：

| 指標 | 第一輪 | 第二輪 | 第三輪 |
|------|--------|--------|--------|
| 首題前耗時 | 240.5 秒 | 208.8 秒 | 139.5 秒 |
| 工具呼叫數 | 30 | 25 | 20 |
| token | 77,730 | 73,237 | 67,636 |
| `WebSearch`／`WebFetch` | 0 | 0 | 0 |

耗時與呼叫數同向（約 7.9 秒／次呼叫），全程無網路等待。**同樣輸入的呼叫數變異達 ±50%**，
故後續驗收一律多輪取平均。實測推翻了「線上核對誤觸」與「精準取用未落實」兩項推論
（皆 0/3），並揪出三個每輪都發生的浪費。

### 變更

- **H1｜時效核對觸發條件改為窮舉三條**（`exam-tutor`、`statute-memorizer`）：原條件「關鍵判斷且
  本地檔首版本日期偏舊」無機械判準——每份 statutes md 檔首版本日期多數距今超過一年，把「偏舊」
  當判準等於每次引用條文都要上網，與同節「預設本地」自相矛盾。改為：使用者明確要求／該檔列於
  `docs/法規版本追蹤.md` 已知過期清單或標「🔴 已知過期」／本地 md 缺漏殘缺。另明文界定
  **statutes 檔首「📌 免責聲明」中的「自行上網查證」不構成上網指示**（該句對象為人類讀者，
  執行期只照做前半段的版本告知）。實測 0/3 觸發上網，本項為消除規範矛盾而非修復已觀測行為；
  **批改階段尚未實測**。
- **N1｜補上「題目參照 → md 路徑」推導**（`exam-tutor`、`exam-archive`）：三輪都在猜
  `corpus/index.json` 欄位名上白花 2–3 次呼叫。全部 160 卷路徑一律為
  `corpus/md/<等別>/<年>/<科目代碼>_<科目名>.md`（已對 160 筆全數驗證），Glob `0809_*.md` 即可，
  **這條路完全不需要讀 `index.json`**；另列出卷內兩種題目標題格式（測驗 `### 第 27 題`、
  申論 `### 第一題（25 分）`）與完整 `papers[]` 欄位，並註明 `year`／`subject` 皆為字串。
- **N2｜條文區段的 `limit` 改由下一條行號算出**（`exam-tutor`、`statute-memorizer`）：三輪中兩輪
  都以 `offset=3022 limit=40` 讀設置標準 §175–§179，但 §179 橫跨 3050–3068 行，被截斷後六款只
  拿到四款（漏第五、六款，含免設處所「洗手間、浴室、盥洗室、儲藏室或機械室」）。原示範
  `limit=60` 等於鼓勵憑感覺估，估錯即靜默截斷。新增「條文區段的邊界」小節：Grep 時多抓下一條
  當右邊界，`limit` ＝行號之差。
- **N3｜出題階段的 `user-config-spec.md` 讀取改為一次取足**（`exam-tutor`、`fs-quiz`、`fs-weak`）：
  三輪都讀了該檔（各 2 次呼叫）即使 `pending` 為空——因為逐題落地要求出題當下就寫 `pending`
  斷點，寫就得有 schema。原措辭「不為了取設定而 Read」給人「出題階段不會讀」的錯覺，改為據實
  描述，並指示直接 `Read offset=87 limit=139` 一次取足。
- **CI 新增 `progress-spec-range` 檢查**：由 `## progress.json` 至 `## 弱點筆記格式` 前一行計算
  實際區段，與 skill 中引用的行號比對，位移即轉紅；只認同一行並存檔名與 `offset` 者，避免誤抓
  同檔中談 statutes 條文區段的範例。已以反向測試確認。

### 已知未處理

- H1 於**批改階段**未測（三輪都只跑到出題）。批改要引用條文全文，時效壓力最大。
- 提案 A（`SKILL.md` 分層載入，該檔 35.5 KB 中批改相關內容出題階段用不到）與提案 B（批次出題：
  3~5 題一次呈現、一次作答、一次批改）均未實作，見 #73。

## [1.2.3] - 2026-08-03 — README 只留使用者內容，開發者說明移入 docs

README 同時混著「使用者要看的」（安裝、指令、權限設定）與「維護者才要看的」（repo 目錄結構、設計原則），
使用者得跳過與自己無關的段落才讀得完。依讀者切分後重組，**內容逐字保留，只換位置**：

### 變更

- `README.md`：移除「設計原則」一節（「目錄結構」依 review 意見留在 README）；文末新增「想更深入？」
  一段，以四個連結（`docs/設計原則.md`、`docs/index.md`、`docs/資料維護.md`、`CHANGELOG.md`）指向開發者文件。
  「為什麼是英文指令名」的緣由移出，只留使用者需要的「打 `/` 後可用中文搜尋」；
  安裝體積說明保留數字，「原始 PDF 刻意入庫」的理由移入設計原則。
  「使用範例」開頭精簡為一句指路（「輸出格式範本請參考 `reference/輸出格式/`」）。
- `README.md` slash command 表：每列功能欄前加上該指令實際的中文名（【抽考】【出考卷】【弱點複習】
  【掌握度】【對照表】【猜題】【申論猜題】【懶人包】【讀書計畫】【備考設定】），與各 SKILL.md
  `description` 開頭一致——使用者打 `/` 後就是用這些字搜尋。
- `README.md` slash command 表新增第四欄「輸出」：每列以 📝 連至該指令輸出格式檔的範例節
  （`/fs-mock` 連至試卷版面結構、`/fs-compare` 連至內建對照表，兩者本身即範例；`/fs-setup`
  為設定對話無輸出範本，留 —）。對應關係取自 `reference/index.md` 的「主要使用者」欄。
- `docs/設計原則.md`（新增）：自 README 移入的五條共同行為準則，另收兩則設計取捨緣由
  （指令名一律用 ASCII、原始 PDF 刻意入庫）。
- `docs/index.md`：設計筆記表首列加入 `設計原則.md`；檔首補上「面向使用者的說明在 README」的指路。

## [1.2.2] - 2026-08-03 — 索引取用規範改為結果導向（收 #66）

[#66](https://github.com/yyc1217/fire-safety-personnel-tutor/issues/66)：規範要求「定位一律用原生 `Grep`
工具、不得用 shell `grep`」，但**三輪真人實測 `Grep` 工具使用次數皆為 0**（shell `grep` 1～8 次），
措辭加強兩次（1.1.0 加警告框與具體呼叫範例）都沒推動。模型偏好先 `cd` 進 plugin 目錄再寫一段一次到位的 shell。

規範與實際行為長期背離，比「用哪個工具」本身更糟，故改為**結果導向**：

### 變更

- `skills/exam-tutor/SKILL.md`「索引與大檔取用原則」與 `statutes/index.md`「大檔取用」：
  **定位手段不限**（原生 `Grep` 工具或 shell `grep` 皆可），真正的規範是**只取所需片段、不整檔載入**——
  這一條模型本來就遵守（三輪實測「大檔整檔 `Read`」皆為 0）。
- 保留並強調兩條路的**代價差異**：原生 `Grep`／`Read` 不需額外授權、不受 Bash 的工作目錄限制；
  經由 Bash 的 `grep`／`jq`／`python3` 每次都要使用者點同意，被拒或環境受限時整條路會斷，
  屆時照退化條改用原生工具，**絕不可因此放棄 corpus／statutes 改為憑記憶出題**（此鐵則不變）。
- `2_01`（4,239 行、超過 Read 預設 2,000 行上限）**必須先定位行號再 Read 帶 offset** 的規定不變，
  只是不再指定用哪種方式定位。

> 附帶記錄：#66／#67 兩輪實測都顯示，Bash 能不能碰 plugin 安裝目錄，取決於 session 的 cwd 是不是
> 它的祖先（從 `~` 啟動時 `~/.claude/plugins/…` 在 cwd 底下，全通），以及使用者有沒有同意權限提示。
> 兩輪都沒能重現 #63 當初「jq／grep 全被擋、模型宣告 corpus 無法讀取」的情況。

## [1.2.1] - 2026-08-03 — 四支 skill 補上「jq 被權限擋下」的退化條

[#67](https://github.com/yyc1217/fire-safety-personnel-tutor/issues/67) 的殘留項。1.1.0（PR #62）只替
exam-tutor 補了這一列，其餘四支仍只寫「`jq` 未安裝 → 改以 `python3 -c` 取單鍵」——而 #67 實測的結論正是
**`python3` 與 `jq` 一樣走 Bash、在預設權限下同樣被沙箱擋下**，所以那條退路在真實使用者身上走不通。

### 修正

- **exam-archive、exam-trend-forecast、statute-memorizer、study-planner** 各補一條「`jq`／`python3`／shell
  指令對 plugin 目錄被權限擋下」的退化處置：**不中止、更不得改為憑記憶作答**，退路是**原生 `Read`／`Grep`
  工具**（不受 Bash 沙箱的工作目錄限制，實測在預設權限下可正常讀取 plugin 目錄），並逐檔給出替代取法——
  `tags_summary.json`（約 90 KB）整檔 `Read`、`tags_index.json`／`tags_cycles.json` 以 Grep 定位後讀該段、
  大檔法規先 Grep 定位行號再 `Read` 帶 offset／limit；同時向使用者說明可依 README 設定
  `additionalDirectories` 與 `allow` 加速。
  - 既有的「`jq` 未安裝 → 改以 `python3`」一列保留：那是 `command not found` 的情境，與被權限擋下不同。
  - statute-memorizer 該列另註明 `progress.json` 在使用者 `data_dir`、不受 plugin 目錄邊界影響。

> #67 的另一半（以 `default` 權限實跑 `/fs-quiz 緊急照明燈`，確認不退化成憑記憶出題）仍需真人驗證，
> headless 一律 `bypassPermissions`、沙箱不擋，跑了不算數。

## [1.2.0] - 2026-08-03 — 收尾 issue #65 的四項已知未處理與 F-2

issue [#65](https://github.com/yyc1217/fire-safety-personnel-tutor/issues/65) 驗收時列為
「已知未處理（不列入本次驗收）」的四項，加上 F-2 留下的簡體字掃描，一次收完。

### 修正

- **整卷模擬考的產出不落地、成績回不到掌握度**（已知未處理 1）：`/fs-mock` 走 `context: fork`，
  子代理無法徵求寫入同意，所以產出當下確實不能寫檔——改為**卷末固定附「保存與批改」區塊**，
  明白告訴使用者「這份卷子還沒存檔」以及兩條後路：說「存這份考卷」→ 主對話徵得同意後寫入
  `<data_dir>/exams/模擬考_<等別><民國年>_<科目>_<YYYY-MM-DD>.md`；貼回作答 → exam-tutor 照常逐題批改、
  **逐題落地**，成績與覆蓋度就此進得了 `progress.json`。
  - `q_id` 新增 `模擬考/<YYYY-MM-DD>#<題號>` 格式（自行命題，不佔 corpus 抽題池、不寫入 `asked_ids`）；
    `data_dir` 新增 `exams/` 目錄。分次批改時未批改的題留在 `pending`，跨對話接得回來。
- **`notes` 模式跨對話要使用者手動回貼**（已知未處理 2）：改為**skill 自己去讀**——需要弱點資料時
  先 `Glob <data_dir>/notes/*弱點筆記.md` 取最近 5 份，以各考點的隱藏 `<!-- tags: ... -->` 註解對回索引、
  以 tag 出現次數代替 `weak_tally` 排序；**Glob 不到檔案時才請使用者提供**，且要講明已經找過哪個路徑。
  弱點筆記本來就是為了「日後讀回」而設計成固定格式的，缺的只是自動去讀這一步。
  `notes` 模式「不寫 `progress.json`」的約定不變——**不寫 ≠ 不讀**，讀到的內容只供本次選題、不回寫。
  弱點複習、掌握度、懶人包三處一致。
- **Claude Code on the web／遠端 session 的進度不保留**（已知未處理 3）：README 補上警告——那些環境的容器
  用完即丟，`data_dir` 跟著消失，改 `data_dir` 路徑也沒用（同樣在容器裡）；要累積進度請用本機 CLI／桌面版，
  非用不可時以 `notes` 模式把弱點筆記帶走、整卷模擬考存檔後複製走。

### 新增

- **CI 檢查 `write-timing`**（已知未處理 4）：掃 `skills/`、`reference/`，攔截「一輪結束才寫入」「輪末統一寫回」
  這類措辭回流（0.11.0 修掉的批次寫入行為，違反鐵則四逐題落地）。判斷會看整句是否為否定敘述，
  故既有的「**不得**累積到本輪結束才寫回」不會誤報。
- **CI 檢查 `simplified-chinese`**（F-2）：掃 `skills/`、`reference/`、`docs/`、README、CHANGELOG 與兩份 manifest，
  攔截簡體字（本 repo 一律正體）。字表以 `statutes/`、`corpus/` 的正體語料反向驗證過——凡在那些檔案中
  出現過的字（伍／斗／蜂／裝／戒／准…）一律不列入黑名單，避免把正體字誤判成簡體。
  起因是 F-2 實測時模型輸出把「掌握度也不會更新」的「會」寫成了簡體；模型輸出掃不到，但規格檔本身可以。
  （這條 CHANGELOG 本身也受該檢查拘束，故此處不引用原字。）

## [1.1.0] - 2026-08-03 — 索引與大檔改為精準取用，冷啟動 token 降約六成

> 本節原以 0.11.0／0.11.1 開發（PR #62），因 1.0.0 先行合併，改以 1.1.0 發布；兩段內容原樣保留。

### 修正 0.11.0（本版前身）在預設權限下的回歸

行為驗證（[#63](https://github.com/yyc1217/fire-safety-personnel-tutor/issues/63) B 層）以 headless 實跑後發現：
**在 `default` 權限下，`jq`／`python3`／shell 指令對 plugin 安裝目錄全部被沙箱擋下，只有原生 `Read`／`Grep` 可通行。**

0.11.0 把 `tags_summary.json` 由「精簡計數，可整檔載入」改成「一律 jq 取單鍵」，等於把一條在預設權限下走得通的路，
換成了會失敗的路。實測結果是模型宣告「corpus 無法讀取」並**直接退化成憑記憶自行出題**——對未設定
`additionalDirectories` 的使用者，0.11.0 是淨負面：原本讀整檔（貴但正確），改動後拿到無法回溯法源的題目。

#### 修正

- **`tags_summary.json` 恢復整檔 `Read` 作為 fallback**（約 90 KB，讀得完）。jq 仍是優先路徑（省 token），
  但不再是唯一路徑。`tags_index.json`（約 370 KB）維持不整檔輸出，補上 jq 不可用時改以 Grep 定位的替代路徑。
- **退化條款新增「被權限擋下」一列**（有別於既有的「未安裝」）：明示改用原生 `Read`／`Grep`，逐檔給出替代取法，
  並要求向使用者說明可依 README 設定 `additionalDirectories` 加速。既有退條的退路是 `python3`，
  但實測顯示它與 jq 一起失效，涵蓋不到這個情況。
- **`Bash(grep *) 會被權限擋下` 的機制描述有誤**，與 `README.md` 打架。`grep` 屬 Claude Code 內建唯讀指令、
  本來就不必列進 allow；真正的擋點是**目錄邊界**——經由 Bash 執行的指令受工作目錄限制，而題庫與法條都在
  plugin 安裝目錄底下。`exam-tutor/SKILL.md` 與 `statutes/index.md` 兩處同樣的錯誤描述均已改正。

#### 新增

- 取用原則開頭加註鐵則：**精準取用是最佳化，不是前提**。任何情況下都不得因為取用手段受限就放棄
  `corpus/`／`statutes/` 而改為憑記憶自行出題——寧可整檔讀、慢一點、貴一點，也不能給出無法回溯到法源的題目。

#### 已知未解（不在本版範圍）

- 模型不照文件使用原生 `Grep` 工具，一律走 shell `grep`（3 個輪次中 Grep 工具使用次數為 0）。
  措辭已寫至明確對比仍未生效，非文件層可解，另於 [#66](https://github.com/yyc1217/fire-safety-personnel-tutor/issues/66) 追蹤。
- `jq` 對 plugin 目錄不可用是**跨 skill** 問題：`exam-archive`、`study-planner`、`statute-memorizer` 的退化條
  同樣只寫了「jq 未安裝」。本版只收斂 0.11.0 自身引入的部分，其餘於
  [#67](https://github.com/yyc1217/fire-safety-personnel-tutor/issues/67) 追蹤。

### 索引與大檔改為精準取用

出題類模式（`/fs-quiz`、`/fs-weak`、連續出題）原本會整檔載入數個索引，其中九成九的內容當輪用不到。
本版把「只取本次用得到的部分」寫成規範，並修掉一個會靜默截斷的取檔風險。

以「`/fs-quiz 緊急照明燈`」一輪估算，冷啟動輸入自 66,700 → 約 23,600 token（估算值，係數為中日韓字元 1.05、
ASCII 0.30 token/字，未經 `count_tokens` 實測）。行為與產出不變——讀進來的資訊相同，只是不再整檔載入。

#### 新增

- `skills/exam-tutor/SKILL.md` 新增「**索引與大檔取用原則**」一節（全模式適用）：逐檔列出取用方式與範例，
  並定義例外判準——「**需不需要比較不同 tag**」，需要就整檔、只查某一個就取單鍵。
  `/fs-mastery`、`/fs-plan`、`/fs-forecast` 等全域統計產出不受限制。
- `statutes/index.md` 新增「**大檔取用**」節，涵蓋所有會讀法條的 skill。

#### 修正

- **`statutes/2_01_各類場所消防安全設備設置標準.md` 整檔 Read 會靜默截斷**：該檔 4,239 行，超過 Read 工具
  預設 2,000 行上限，整檔讀取只拿得到前半部且無任何錯誤提示。後果是查後半部條文（§175 緊急照明、
  §180 以後之公共危險物品專章等）時誤判「查無此條」，或引用到不完整條文。已於兩處寫明必須走
  Grep 工具定位行號 → Read 帶 offset／limit 的流程。本目錄其餘 md 均在上限內。
- 定位與取段一律用 **Grep 工具**而非 shell `grep` 指令，並就地標注原因：各 skill 的 `allowed-tools`
  只授權 `Bash(jq *)` 與 `Bash(pdftoppm *)`，`Bash(grep *)` 會被權限擋下。

#### 變更

- `corpus/tags_summary.json` 由「可整檔載入」改為**單一 tag 用 jq 取單鍵**（單鍵 160 tok vs 整檔 27,922 tok）。
  跨 tag 排序的場合（如 exam-tutor 選擇設備時比較各設備頻率）維持整檔，並就地標注屬例外。
- `reference/索引/設備條文索引.md` 由整檔 Read 改為**單一設備用 grep 取該設備列**（225 tok vs 10,146 tok）。
- `reference/user-config-spec.md` **延後到需要時才讀**，不在開場載入：設定解析順序已寫在各 slash command
  與 exam-tutor SKILL.md 內，出題階段不需要該規格檔。僅在要寫 `progress.json`、跑初次詢問流程、
  或既有 `progress.json` 結構不符預期需查遷移規則時才讀。`/fs-quiz`、`/fs-weak` 同步調整。

#### 未納入

- 「把設定解析整個移到伺服器端」屬 web app 架構層（Agent SDK 之 userConfig 代入層）的做法，
  在 plugin 內沒有對應元件——Claude Code 就是 runtime，沒有可代勞的伺服器。故本版只做「延後讀取」，
  不做「不讀」。
## [1.0.0] - 2026-08-03 — 十支 slash command 改 ASCII 名稱；鐵則六擴及非出題 skill

> **為何跳 MAJOR**：指令改名對既有使用者是破壞性變更（舊的中文指令名一律失效），依
> `.claude/CLAUDE.md`「破壞性變更升 MAJOR」升至 1.0.0。

### 變更（破壞性：舊的中文指令名不再可用）

- **十支中文指令名一律改為 `fs-` 前綴的 ASCII 名**。Claude Code（實測版本 2.1.220）在註冊 plugin 指令時，
  會把 skill 名稱中的**非 ASCII 字元逐字換成 `-`**：`/抽考` 註冊成 `/--`、`/掌握度` 成 `/---`、
  `/申論猜題` 成 `/----`，同字數的名字互相蓋掉，**十支只有三支叫得動**（`/抽考`、`/掌握度`、`/猜題`
  各自搶到 `--`／`---`／`--`），其餘七支不會展開，原文當純文字送進對話。
  判準為 CLI 自己寫的 session transcript 有無 `<command-name>` 標籤（issue #65 第三輪）。

  | 舊 | 新 | 舊 | 新 |
  |----|----|----|----|
  | `/抽考` | `/fs-quiz` | `/懶人包` | `/fs-cram` |
  | `/掌握度` | `/fs-mastery` | `/對照表` | `/fs-compare` |
  | `/猜題` | `/fs-forecast` | `/弱點複習` | `/fs-weak` |
  | `/申論猜題` | `/fs-essay` | `/備考設定` | `/fs-setup` |
  | `/讀書計畫` | `/fs-plan` | `/出考卷` | `/fs-mock` |

  - **中文仍找得到**：每支指令的 `description` 開頭保留【抽考】【掌握度】等中文名，打 `/` 後輸入中文即可搜到。
  - **不記指令也能用**：README 新增觸發語對照表（「考我幾題」「我的掌握度」「幫我排讀書計畫」…）。
    惟 `/fs-setup`（設定無對應 skill）與 `/fs-mock` 的整卷模擬考（`context: fork`）**只有指令能觸發**。
  - `skills/` 下十個資料夾同步更名，全庫 30 個檔案內的指令引用一併更新。

### 修正

- **鐵則六擴及非出題 skill**（issue #65 第三輪 H-6）：規範原本只在「續作提示」與「出題 skill 的續作偵測」
  底下談洩題，study-planner 產進度報告時不跑續作偵測，其條號是照自己 SKILL.md「銜接既有進度」讀
  `coverage` 帶出來的——兩份規範各自都被遵守，合起來卻在同一則回覆裡把三題未批改題的 10 個條號講完
  （「室內消防栓（§15、§32–§38、§209）的設置標準都出過題了」）。
  - `reference/user-config-spec.md` 鐵則六補明：判準是「當下 `pending` 有沒有未批改題」而**非**「這次是不是
    出題流程」，study-planner／statute-memorizer／exam-trend-forecast 同樣受拘束；要列條號前先確認 `pending` 為空。
  - `skills/study-planner/SKILL.md` §2.7、§5 各補一句：`pending` 非空時整則回覆只寫到〈設備〉層級、不列條號。

- **掌握度設備分母漏算延伸知識考點**（issue #65 第三輪 H-4 附帶）：滅火器顯示 `0／9`（設置標準 6＋
  檢修基準 3），但規範定義的分母是「該設備全部條文＋文末延伸知識考點」，正確值為 **10**。根因是
  `reference/索引/設備條文索引.md` 之「延伸知識考點」表把設備寫成 `滅火器／乾粉滅火設備`（併寫）、
  `二氧化碳滅火設備`（條文表用字為 `二氧化碳及惰性氣體`）、`消防幫浦`（不是設備 key），名稱對不上
  就整列漏算。已改為單一設備名且與條文表用字一致（併寫者拆兩列、幫浦歸 `室內消防栓` 並標「水系統
  共通、分母只計一處」），並於索引與 `掌握度視覺化.md` 明寫分母算法與「自行擴充之延伸考點不計入分母」。

### 新增

- **CI 檢查 `slash-commands`**（`scripts/ci_check_repo.py`）：skill 的 `name:` 與資料夾名須為 ASCII 且一致；
  文件中引用的 `/fs-*` 指令須實際存在；文件（CHANGELOG 之歷史紀錄除外）不得再出現中文 slash command。
- **CI 檢查 `equipment-index`**：「延伸知識考點」表的設備名須與條文表用字完全一致，防止分母再度漏算。

## [0.11.0] - 2026-07-29 — 跨對話（session）狀態：逐題落地、未批改斷點與併發

原設計把使用者資料落在 `data_dir`，**檔案層本來就跨得過去**（設定、弱點、覆蓋度、讀書計畫都在），
但**對話狀態層跨不過去**：出題後等作答的那一題只活在對話脈絡裡，關掉視窗、`/clear`、脈絡壓縮或
雲端 session 容器回收之後，新對話無從得知「上次問到哪一題」。九項落差之完整盤點與設計取捨見
[`docs/設計_跨session狀態.md`](docs/設計_跨session狀態.md)。

### 新增

- **未批改斷點 `pending`（跨對話續作）**：`auto` 模式出題時即把該題寫入 `progress.json` 之 `pending`
  （題目全文、`q_id`、來源／題型／配分、所考條號、本輪已完成題數與成績），批改完移除、一輪結束刪掉整節。
  **存題目全文而非只存 `q_id`**——本 plugin 有相當比例的題是改編或就地命題，只存參照的話這些題還原不回來。
  - **斷點嚴禁含答案**：不得寫入正解、評分要點或法源提示（`pending` 就在使用者檔案系統裡，翻得到），
    否則牴觸「先問、後等、再解」。
  - **續作偵測**（各出題 skill 開始正題前執行）六條規則：有效斷點給「接續／重開／先看掌握度」三選項；
    逾 14 天預設重開；`session_id` 非本對話且未滿 30 分鐘視為另一視窗正在練習，先問再動；等別不符預設丟棄；
    選重開時保留 `asked_ids` 以免又抽到同一題。
- **`attempts.jsonl`**：逐題作答明細改為一題一行、只 append 不重寫的 JSONL。整檔重寫在中斷或多視窗並行時
  會整批損毀，append 最多損失當下那一行。載入時只讀 `progress.json`，需要明細才取 jsonl 尾端 N 行。
- **`schema_version`**（現行 3）：取代原本「看到沒有 `asked` 欄就猜是舊檔」的啟發式判斷，既有兩條遷移規則
  （`done`→`asked`、`total`→`points`）改掛在版本號下；v2→v3 遷移把舊的 `attempts` 陣列逐筆轉入 jsonl。
  遷移一律只增不刪，解析失敗時保留原檔並如實告知。

### 修正

- **🔴 寫入時機由「輪末」改為「逐題落地」**：`modes/弱點複習.md` 原本寫「結束時總結；auto 模式寫回
  progress.json」，與 `exam-tutor/SKILL.md`「批改時寫 coverage」互相矛盾，實作上會取較晚者——一輪 5 題
  做到第 4 題被打斷即**整輪歸零**。改為出題時寫斷點、每題批改完寫成績，輪末只做呈現（成績摘要、弱點總結）。
  新增為 `user-config-spec.md` 鐵則四。
- **併發覆寫**：`progress.json` 原本是整檔重寫，兩個視窗（或主對話與 `/出考卷` 的 fork 子代理）同時寫，
  後者會蓋掉前者。改為**每次寫入前先重讀再合併**，並以 `session_id` 偵測 30 分鐘內的另一視窗。
  未採檔案鎖——成本高於效益，理由見設計筆記。
- **拒絕寫入時不再靜默**：「優雅退場」表補四列（拒絕 `Write` 權限、斷點過期／等別不符、斷點屬另一視窗、
  進度檔損毀）。使用者按「否」時必須當場明講「本題未寫入進度檔，關掉對話就接不回來」——以為有記錄、
  實際沒有是最糟的結果。

### 附帶澄清

`Write` 不列入各 skill 的 `allowed-tools` **不會**擋掉寫檔。官方文件對該欄位的定義是「Tools Claude can use
**without asking permission** during the turn that invokes this skill」，是預先授權而非限制（限制用的是
`disallowed-tools`）。逐次徵詢同意是刻意設計，能力並未缺席；只是 `auto` 模式改逐題落地後提示會變密集，
README 權限提示節已補說明。

### 驗收修正（issue #65 實測回饋）

合併前的 A～I 區驗收（issue #65）確認核心保證成立（逐題落地 C-4、跨對話續作 D-3／D-5、schema 遷移
B-1／B-2 全數通過），另揪出六項規格缺口，於本版一併修掉：

- **🔴 拒絕寫入一次後整場靜默停寫**（F 區新發現）：原規格只寫「被拒時要明講」，沒規範**拒絕之後怎麼辦**，
  實測第 1 題按「否」之後，後續每一題都不再嘗試寫入、也不再提醒。使用者只在第 1 題看過一次提醒，
  會理所當然以為後面都有記，跑 `/掌握度` 才發現一片空白。**只要按一次「否」就會遇到**，故列為最高優先。
  改為：每次寫入照常嘗試；同一輪連續 2 次以上被拒才一次性問「本輪都不要記錄嗎？」，明確答覆才停止；
  停止期間每輪結束仍須再提醒一次。
- **❌ 續作提示洩題**（D-4，三次續作複現兩次）：`pending.items[].articles` 是內部指標，原規格把
  「續作時不得顯示」寫在欄位表的一句附註裡，而 `exam-tutor/SKILL.md` 的續作偵測段完全沒提——
  Claude 手上有整個 `pending` 物件，很自然就把條號、配分一起當上下文報出來（「上次在『室內消防栓設備』
  （設置標準第 209 條）出的一題…」）。**申論題只要知道考哪一條，等於先給了答題骨架**。
  升格為**鐵則六（續作不得洩題．顯示端）**——鐵則五只管「存」，這條管「顯示」——並明訂續作提示的
  可顯示範圍只有〈設備〉、模式、「M／N 題」與 `question_text` 原文，同步寫進 SKILL.md 續作偵測段。
- **⚠️ 併發衝突時退回輪末批次寫入**（G-1）：兩視窗同時批改時，發現 `pending` 被他人換掉的那一方
  停下整個進度檔寫入，並提議「最後才一次把 4 題補寫回去」——正是鐵則四要消滅的行為。
  改為**衝突只鎖 `pending`**：`attempts.jsonl` 照常 append（append-only，零衝突）、`weak_tally`／
  `coverage` 照常重讀合併，只有 `pending` 暫不寫入並詢問使用者。
- **⚠️ 損毀的 `progress.json` 遭就地覆寫且全程未告知**（B-6／F-3）：這次資料零損失（損毀輕微、修得回來），
  但就地覆寫會把壞掉的證據一併蓋掉，使用者連手動搶救的機會都沒有。改為**先另存
  `progress.json.corrupt-<YYYY-MM-DD>` 才可動原路徑**，且必須當場告知檔案曾壞掉、原檔存在哪、救回了什麼。
- **另一視窗偵測改以內容比對為主判準**（G-3）：實測認出他人斷點靠的**不是 `session_id`**（該欄可能缺值，
  巢狀 session 甚至共用同一 id），而是「檔案裡的 `pending` 是別的設備、別的題目，不是我這輪在做的」。
  規格改以內容比對為主，`session_id` 與 30 分鐘窗口降為輔助條件。
- **斷點時間戳改取系統時間**：模型只拿得到 UTC 基準的日期，自行拼出的時分秒與時區是假的——
  UTC+8 晚間寫下的斷點，隔天看來會像「未來時間」（E-2 實際踩到）。`Bash(date *)` 加入
  `exam-tutor`／`/抽考`／`/弱點複習` 的 `allowed-tools`（唯讀、無副作用），取不到時只寫日期並跳過 30 分鐘窗口。

一併修掉三項體感問題：

- **寫入密度**：實測每題約 4.5 次寫入（12 題寫了 41 次 `progress.json`），README 原本寫的「出題與批改
  各一次」是低估。規格新增「**每題最多兩次 `progress.json` 寫入**」——同一時機的多個欄位必須併成一次
  （拆寫只是把權限提示次數乘上去，落地即時性一點也沒多），README 改為據實說明每題 2～3 次。
- **自出題 `q_id` 會被重編**（C 區觀察）：同兩題在改寫 `progress.json` 時從 `#1`／`#2` 變成 `#6`／`#7`，
  後續新題又拿到 `#6`。明訂序號＝當日已用最大序號＋1，**一經寫入即固定不得重編**。
- **無斷點時多播報一句**（D-6）：「先前沒有未批改的斷點，直接開始新一輪」這種「我查過了，沒有喔」的
  內部狀態播報，每次抽考都來一句是雜訊。規格改為明確禁止提及。

修正後的複測（同一份 issue，第二輪）又揪出三項，一併修掉：

- **❌ 鐵則六被「照字面遵守」繞過**（D-4 第三次取樣）：斷點提示區塊寫得完全乾淨，**緊接在後的
  「建議接下來練哪裡」卻點名 `設置標準第73條`（局部放射防護面積、放射量）與 `檢修基準第七章`
  （發泡倍率、25% 還原時間）**——正是那兩題還沒作答的考點。該段取自 `weak_tally`／`coverage`，
  不在原鐵則六管的「續作提示」範圍內。**鐵則六改為管整則回覆**：只要 `pending` 還有未批改的題，
  同一則訊息任何段落都不得點到那些題的 `articles`，未批改期間弱點與覆蓋建議只講到〈設備〉層級。
  （前兩次取樣都是乾淨的，正是「至少跑三次」才抓得到。）
- **❌ 續作三選項自帶洩題出口**：選項三原本主動推薦 `/掌握度`，而它按條文列覆蓋度，等於繞道洩掉
  同一批未批改題的考點。改為「先丟著、做別的」，使用者自己要看則先告知風險再由他決定。
- **⚠️ 自出題 `q_id` 在併發下碰號**（G-1 實測）：兩個視窗同時出題，各自算出「當日最大值＋1」而
  **同時產生 `自出題/2026-07-31#1`**，兩道不同的題共用一個 `q_id`（即上一版才修掉的「一題兩號」的鏡像）。
  格式改為 `自出題/<日期>#<序號>-<4碼隨機字尾>`，不需跨視窗協調即可避免碰號。

### 尚未處理（後續版本）

`/出考卷`（`context: fork`）產出不落地、`notes` 模式跨對話仍靠手動回貼、雲端 session 之 `data_dir` 為
ephemeral 需於 README 說明、以及對應的 CI 防迴歸檢查——四項均列於設計筆記文末。

issue #65 之 H-1（`none` 模式完全不寫）、H-2（`notes` 模式行為不變）已於第二輪補驗通過；
尚有 H-4、H-6、H-7 未跑，以及 F-2 實測輸出出現零星簡體字（可考慮於 CI 加簡體字掃描）——
均待後續補驗，不阻擋本版合併。

第二輪複測另記一項**未修**的輕微偏差：續作提示會在每題前標「（申論，未批改）」，而鐵則六的白名單
不含題型。因 `question_text` 就在下方、申論與否一望即知，且真正有訊息量的配分已正確略去，
評估為無害，留待後續版本再決定要收緊規格還是放寬白名單。

## [0.10.0] - 2026-07-25

本版含兩批工作：**發布層與 plugin 規格體檢**（原標 0.9.0，因 `main` 已於 2026-07-22 發布 0.9.0「考點重考率×顆粒度分析」而併入本版）＋ **skill 結構重整**。

### 修正

- **🔴 十個中文斜線指令全部叫不起來**（issue #59 實測發現）：`/抽考`、`/猜題`、`/掌握度`、`/讀書計畫`、`/出考卷`、`/備考設定`、`/弱點複習`、`/申論猜題`、`/懶人包`、`/對照表` 皆未註冊，叫用時回 `Unknown skill ... Did you mean fire-safety-personnel-tutor:--?`（`--` 即指令名推導失敗的殘骸）。**README 宣傳的整組斜線指令等於失效**，只有 5 支 ASCII 命名的 skill（`exam-tutor` 等）可用。
  - **根因**：plugin skill 的**目錄名為非 ASCII、且 frontmatter 未設 `name:` 時**，指令名推導失敗而不註冊。以合成 plugin 逐一拆變因驗證：無 `name:`＋ASCII 目錄 ✅、有 `name:`＋ASCII ✅、有 `name:`＋中文 ✅、**無 `name:`＋中文 ❌**。本 plugin 的 5 支 ASCII skill 都有 `name:`，10 支中文指令都沒有。
  - **修法**：十個 `SKILL.md` 各補一行 `name: <目錄名>`。**中文指令名本身沒有問題**，補上 `name:` 即可，不必改成英文。
  - 附帶確認：修好後 `/備考設定` 立即可用並正確印出代入值（`level: 士`），**同時實證了本版 `${user_config.*}` 代入區塊的修正在指令檔上確實生效**。

### 新增

- **導入 plugin `userConfig`**：應考等別、弱點記錄模式與學習資料目錄改為可在 Claude Code 的 `/plugin` 設定對話框直接填寫，值由 Claude Code 代入各 skill 內文（`${user_config.*}`），不必等到第一次對話才被問。
  - 三項來源的取用順序統一定義於 `reference/user-config-spec.md` 之「**設定解析順序**」：plugin 設定 → `<data_dir>/config.json` → 初次詢問流程。**既有使用者的 `config.json` 完全不受影響**，仍是第二順位且必須支援。
  - 留空即維持原本的互動式詢問（對話能說明師／士與三種記錄模式的差異，設定對話框做不到），故不對等別與記錄模式設預設值。
  - `exam_date`、`weekly_hours`、`progress_reminder` 仍只存 `config.json`——`exam_date` 需要「推算下一個六月第一個週六再請使用者確認」的互動，設定對話框無法勝任。
  - `/備考設定` 改為並列顯示兩處設定、標明何者生效，並明白告知**本指令改不動 plugin 設定**（該處要去 `/plugin` 改），不假裝寫入成功。
  - **代入區塊補進全部 user-invocable 指令**（issue #59 驗證發現）：`${user_config.*}` 只在「**被叫用的那份 SKILL.md**」內文代入，模式檔與 `reference/` 檔案由 Read 讀入、佔位符不代入。原先只有 `exam-tutor`／`exam-trend-forecast`／`statute-memorizer`／`study-planner`／`備考設定` 帶有代入區塊，而使用者實際打的是 `/抽考`、`/出考卷` 等指令檔，故順序 1 取不到值。現於 `抽考`、`掌握度`、`弱點複習`、`申論猜題`、`出考卷`、`猜題`、`懶人包`、`讀書計畫` 八支指令各補上代入區塊。
    - **`/出考卷` 影響最大**：它以 `context: fork` 執行，看不到主對話也無法提問，修正前只在 `/plugin` 對話框填過等別、且無 `config.json` 的使用者會拿到**預設「師」的卷**而不自知。
    - `user-config-spec.md` 同步載明此界線，並明訂「看到字面 `${user_config.level}` **不等於未設定**」，避免誤入初次詢問流程。
    - **CI 補上防迴歸檢查**（`plugin-paths`）：正文提及 `user-config-spec.md`／`<data_dir>`／`config.json`／`progress.json` 的 `SKILL.md` 若缺代入區塊即 FAIL；`SKILL.md` 以外的檔案（模式檔等）出現 `${user_config.*}` 亦 FAIL——與既有的「`reference/` 不得使用」同一條界線的另外兩半。判斷「這支是否會用到設定」只看代入區塊**之前**的正文，否則區塊自身提及 `<data_dir>` 會使判斷循環。

### 變更

- **`/出考卷` 改在獨立子代理中執行**（`context: fork` ＋ `background: false`）：整卷命題約需 5–8 分鐘、過程要翻大量條文與考古題，現在全部隔離在子代理裡，**只有完成的試卷回到主對話**。仍是同步等待（不改變「跑完才給你」的體感），但主對話不再被命題過程的中間讀取塞滿。
  - 代價誠實說明：子代理**看不到主對話歷史、也無法中途提問**。因此 `/出考卷` 未指定科目時不再詢問，改依「`$ARGUMENTS` → 讀書計畫下一個未完成單元 → 最弱考點 → 該等別最高頻科目」自行選定，並**於卷首說明選這科的理由**與如何指定科目。等別取不到時預設以「師」出卷並於卷首標明。
  - 在一般對話中說「出一份模擬考」走的仍是原路徑（不 fork），該詢問時照樣詢問。

- **`exam-tutor` 模式拆檔**：`SKILL.md` 一旦叫用就整份常駐 session，但其中六個模式彼此互斥——打 `/抽考` 卻要一併載入「整卷模擬考」與「申論擬答」的全部規範。現在 SKILL.md 只留**共用部分**（角色、資料來源、出題與批改主線、法條時效核對與條號要旨護欄、學習進度、優雅退場）＋**模式路由表**，六個模式移至 `skills/exam-tutor/modes/`，進入模式時才讀。SKILL.md 由 15,431 字降至 10,043 字（**−35%**），只用單一模式的指令不再載入其餘五個模式的規範。
  - 一併把原本只寫在「複習模式」段落、實際上「批改、申論擬答、弱點複習一體適用」的**條號要旨護欄**提升為 SKILL.md 的全模式共用規範。
  - `/抽考`、`/弱點複習`、`/掌握度`、`/申論猜題`、`/出考卷` 五個指令改為直接指向各自的模式檔。
  - CI 新增 `plugin-paths` 檢查：skill 內文引用的 `${CLAUDE_PLUGIN_ROOT}/...` 路徑須存在，且模式檔不得成為沒有任何路由指向的孤兒。

- **README 更正 `allowed-tools` 的效力範圍**（issue #59）：原本寫「這些唯讀查詢不會反覆跳權限提示」與事實不符。`allowed-tools` 的授權**只涵蓋叫用該指令的那一個對話回合，使用者送出下一則訊息即失效**，故 `/抽考`、`/弱點複習` 這類一題一答的多回合流程，第二題起仍會再次詢問（`/掌握度`、`/出考卷` 等單回合產出不受影響）。
  - **plugin 無法代為設定權限**（Claude Code 的安全邊界，plugin.json 無此欄位、plugin 提供的 agent 亦不得指定 `permissionMode`），因此改為在 README 新增「權限提示」節，提供使用者自行加在 `~/.claude/settings.json` 的 allow 規則範例（`Bash(jq *)`、`Bash(pdftoppm *)` 與 `permissions.additionalDirectories`）。
  - 維持 `Write`／`Edit` 不預先授權的設計，並於 README 明白建議使用者**不要**把它們加進 allow 規則——寫入學習資料每次都該讓使用者看到。
  - **範例規則補上 plugin 安裝目錄**（issue #59 測項 D-1② 實測不通過）：原範例的 `additionalDirectories` 只列 `~/.fire-safety-tutor`，照著設定後 `/抽考` 仍有數次詢問。實測釐清兩個欄位管的是不同的事——`allow` 管「指令可不可以跑」，`additionalDirectories` 管「可不可以碰這個目錄」，而**題庫、法條全文與索引都在 plugin 自己的安裝目錄底下**，通常不是使用者的工作目錄，所以指令放行了、讀取仍會逐次詢問。以拆變因驗證：只給 `Bash(grep *)` 等指令規則 → 仍全數詢問；只給 plugin 目錄的 `additionalDirectories`（不給任何指令規則）→ 完全不詢問。範例改為同時列出學習資料目錄與 plugin 安裝目錄，並註明 `ls`／`cat`／`grep`／`wc`／`head` 屬 Claude Code 內建唯讀清單、不必列入 `allow`（`jq`／`pdftoppm` 才需要）。

## [0.9.0] - 2026-07-22 — 考點重考率×顆粒度分析

新增探索性分析工具，驗證備考坊間口訣「前一年考過的考點今年不會再考」之可信度，**分等級（師／士）與考科**檢視。

- **新增** `scripts/analyze_repeat_granularity.py`＋詞庫 `scripts/repeat_lexicon.json`：以**四層顆粒度**（設備／組件／子考點／子子考點）計算「隔年重考率」，並以**置換檢定**（打散年份標籤 3000 次求隨機基準）判斷觀測重考率是否**顯著低於隨機**（＝是否真有「迴避去年考點」效應），而非只是切得越細、格子越多的機率假象。
- **產出** `corpus/命題重考率分析.md`（人可讀）與 `corpus/repeat_granularity.json`（機器可讀）。涵蓋**水系統、化學系統、警報系統、避難系統**。
- **方法學與誠實警語**見 `docs/設計_重考率與顆粒度分析.md`。
- **主要結論**：粗顆粒度（設備／組件）核心考點**年年回鍋**；細顆粒度重考率下降但（樣本足夠時）**與隨機基準無異**——下降純屬顆粒度機率，非命題者刻意迴避。故此口訣**當篩題規則不可信**，僅在「同一核心組件今年多換一個子考點角度切入」意義上近似成立。
- **界線**：子考點以下採**關鍵字啟發式**分桶（準確度有限，同 `build_tags.py` 之停用教訓），本工具**絕不回寫** `tags_index.json`／inline `🏷️` 語意標籤；語意標籤仍為唯一真相來源。
- **連帶調整猜題「冷卻中」型態（軟化動作）**：以「留出未來」回測發現「冷卻中」考點之實際再考率**並不低於**「週期到期」（法條層甚至更高），gap_now 於法條／設備粗顆粒度近乎無預測力。故 `exam-trend-forecast/SKILL.md` 與 `reference/輸出格式/猜題報告格式.md` 將冷卻中由「**降權**」改為「**不降權、維持基礎頻率**」，僅保留標記供出題換角度之提示；`analyze_cycles.py` 之分型態邏輯不動。
- **申論猜題補「換角度重出」指引**：`exam-tutor/SKILL.md` 申論猜題＋擬答模式新增——對高頻回鍋之核心考點，預測題目時優先選近 1–2 年未出現過之**子考點角度**（換角度重出），不因去年考過而剔除核心考點。依據同上（子考點層非重考率高、設備／組件層年年考）。`申論猜題` 本身無冷卻中降權邏輯（沿用 `/猜題` 或基礎頻率），故不需修正。
- **補齊漏設之設備桶**：全面盤點各系統相關科目之 `by_equipment` 標的後，補入原漏設之設備桶（大宗「未落桶」多為士合併卷之跨系統題溢出，屬正常，非缺漏）：化學系統補 **滅火器**（16/16 年年考，含設備＋組件桶）、水系統補 **消防幫浦/加壓送水裝置（共用）**、警報系統補 **手動報警設備**。防災監控系統綜合操作裝置因師卷幾乎未出（≤1 題）暫不設桶。化學設備層落桶率由 32% 升至 41%。
- **新增「公共危險物品場所」系統**：此為法規／場所領域（非設備系統），跨消防法規(師/士)＋水化學(士)＋化學(師) 共 366 題，各層均加 hazmat `_gate` 只分析公危險物品題；層名沿用設備/組件/子考點框架，語意為場所大類→主題→焦點(儲槽場所)。結論同型態：核心大類（製造/處理/販賣場所、管制量、滅火困難分級與應設設備、保安距離、防液堤）年年回鍋。過程修正兩處分桶陷阱：法規全名「可燃性高壓氣體」造成之假性命中、儲槽子考點過寬之大雜燴（未分類 38%→8%）。未分類率：設備 18%／組件 14%／子考點 8%。
- **報告全面改為「先分師／士、再分系統」編排**：依「各科／各等別獨立」鐵則，`analyze_repeat_granularity.py` 之報告重構為 **師篇 → 各系統（階梯＋該等別桶分布）→ 師篇小結／實務建議；士篇同；末附兩等別共通裁決**。桶分布與未分類率亦改為**分等別**呈現（`analyze_layer` 改記各 scope 之桶與未分類；合併數據仍存 JSON 供查，不作主要判讀）。結論與實務建議分師（申論）／士（測驗）各一版。
- **四系統分師後再檢視（合併掩蓋之訊號）**：分等別後多處呈「**顯著高於隨機**」（比隨機更常重考），為合併時被平均掉者——師化學設備/組件（百分位 ~97–100）、師避難設備（~100）、師水系統組件（97）、士警報設備（98）、公危險物品師子考點與士組件/子考點（95–97）。方向與「迴避」相反，更強力反證此口訣。唯一「顯著低於隨機」者仍為水系統**子子考點**（師/士，樣本極小、事後多重比較，多屬雜訊）。
- **實務建議改為「依統計顯著性驅動」**：`render_conclusion` 之實務建議不再統整所有桶，改由置換檢定百分位動態生成——🔺 **顯著高於隨機**（>95）之系統×層級才列入「優先準備」並點名該層年年回鍋核心（師申論導向繪圖/計算/換角度、士測驗導向背熟數值）；🔻 **顯著低於隨機**（<5）列「可略降優先」（附樣本過小警語）；➖ 與隨機無異者明示「依基礎頻率、勿因去年考過增減」。方向性建議一律以顯著性為據。

## [0.8.0] - 2026-07-16 — 掌握度視覺化（PR #26）

新增**掌握度視覺化模式**（`/掌握度`）：把本機作答紀錄（`progress.json`）疊到題庫分類骨架上，畫成各科目／主題／設備／法條的熟悉度文字條地圖（版本 → `0.8.0`）：

- ✅ **`/掌握度` slash command**：薄封裝（比照 `/弱點複習`），轉呼叫 exam-tutor 新增之「掌握度視覺化模式」；本模式**唯讀不寫檔**。
- ✅ **掌握度＝內容覆蓋度**（非答題得分率）：每個主題／法條／設備以「相關內容清單」為分母，掌握度＝已展現掌握之內容點 ÷ 清單總數；尚未接觸以 0% 計、全部項目列出（完整地圖）。
- ✅ **三種圖表**：🔥 火災學 19 主題 20 格實心條＋覆蓋計數欄；📜 消防法規前 5 大法規全條文每 10 條一列之 8 級量化方塊熱區；🧯 消防安全設備依水／化學／避難／警報四系統逐設備文字條。格式唯一真相＝`reference/輸出格式/掌握度圖表格式.md`。
- ✅ **批改時算好、呈現時直接拿**：分子 `done` 與法條分母 `total`（語意切分之獨立要點數）由 exam-tutor 批改時存入 `coverage`，呈現時零重掃 statutes。
- ✅ **`coverage` 語意拆分**：設備項 `asked`（已問過＝出題進度）／`done`（批改判定已掌握）分開記錄；舊檔（無 `asked` 欄）遷移規則入 `user-config-spec.md`；study-planner 明訂只讀 bare-key 設備項、忽略 `by_article:`／`by_topic:` 前綴 key。
- ✅ **新增分母骨架索引**：`reference/索引/火災學主題知識點索引.md`（19 主題知識點清單）、`reference/索引/法規條文清單索引.md`＋`scripts/build_article_list.py`（前 5 大法規條文骨架，腳本可重跑）。

## [0.7.0] - 2026-07-13 — 輸出格式範本補齊

盤點各功能產出後發現：僅 `/出考卷` 與 `/猜題` 有格式範本，其餘輸出只有零散規則、每次輸出長相會漂。比照「範本檔＋SKILL 指向逐區塊比照」的既有慣例，依優先序補齊（版本 → `0.7.0`）：

- ✅ **🔴 批改輸出格式**：新增 `reference/批改輸出格式.md`——申論／選擇批改區塊、法源標注統一格式（全名＋版本日期、簡稱規則、非法源標注）、快速抽考精簡講解、輪末成績摘要、弱點複習結束總結；exam-tutor 四處指向。
- ✅ **🔴 弱點筆記格式**：`user-config-spec.md` 新增專節——`notes` 模式筆記要**讀回選題**，每考點固定 `tag：` 行（對齊 `tags_index.json` 維度）與 `題目參照：` 行（`q_id` 格式），讀回只認 `tag：` 行。
- ✅ **🔴 懶人包格式**：新增 `reference/懶人包格式.md`——科目分節、弱點考點排最前（`⚠` 前綴且不占配額）、必背一行一事實附法源、修法新點／記憶術／對照表出處欄、規模取捨（每考點 3～6 行）。
- ✅ **🔴 擬答格式**：新增 `reference/擬答格式.md`——每題「題目→答題架構→擬答→本題法源清單」四區塊、題目版面比照 `試卷格式.md` 並附一句預測理由、題數依卷型。
- ✅ **🟡 讀書計畫檔格式**：`user-config-spec.md` 新增專節——逐週排程補固定版面（原僅順序清單有範例）、單元行三段式、勾銷＝`[x]`＋行尾 `✅日期`、重排保留已勾銷行、啟動時進度報告固定三行。
- ✅ **🟡 記憶整理版面**：statute-memorizer 新增「各產出形式固定版面」節——記憶卡（表格、答附法源）、口訣（口訣句＋逐字對應）、易混淆組（並列表格＋一句點破）。
- ✅ **🟢 低優先**：即時生成對照表明文「版面比照內建表」；exam-archive 列清單表格欄位（`✓`／`✓答`／`✓更`／`—`）與查單題呈現格式固定；exam-tutor 複習模式輸出分節固定（設置規定→檢修要領→常考數字→自我檢測）。
- ✅ **格式範本集中、格式與範例同檔**：輸出格式檔集中至 `reference/輸出格式/`（共 10 檔），**每檔＝格式定義＋文末範例節**：出題呈現、試卷（本身即版面範例）、批改輸出、猜題報告、擬答、懶人包、弱點筆記、讀書計畫、記憶整理、題庫查詢與複習。原散落於 exam-tutor（出題呈現）、user-config-spec（弱點筆記、讀書計畫）、statute-memorizer（記憶整理）、exam-archive（清單／查單題）之格式定義抽出成檔，來源處改為指向；既有兩份範例（出題練習、猜題報告）併入對應格式檔。全部路徑引用同步更新（README、`docs/index.md`、`reference/index.md`、各 SKILL）。

## [0.6.2] - 2026-07-13 — plugin 體檢修正

全面體檢 plugin（`.claude-plugin/`、14 個 skill、reference 範本、README）之矛盾／錯字／過時敘述，所有發現均以 repo 內實際資料（`corpus/index.json`、`tags_cycles.json`、`tags_summary.json`）驗證後修正（版本 → `0.6.2`）：

- ✅ **🔴 `試卷格式.md` 卷型錯誤**：三處把「師火災學」誤寫為混合卷（corpus 證實師火災學歷年皆純申論，僅消防法規混合）；其中第三節是 `/出考卷` 選卷型的直接依據，會產出錯誤卷型。已改正為「師消防法規及士全部科目→混合卷；師火災學及四系統→純申論卷」。
- ✅ **🔴 `skills/猜題/SKILL.md` 未跟上兩段式改版（PR #21）**：原文仍指示無條件檢索 12–24 個月動態（實為可選第二段）、報告分「法規脈動／技術實務／時事預測／回鍋與缺口」四層（實為考試科目分層）、每項含 tag 與信心程度（格式明定不露代碼、不列信心）。已依 `猜題報告格式.md` 重寫。
- ✅ **🔴 「信心程度」殘留字樣**：exam-trend-forecast（description、1-1 表、指路段、誠實性護欄）與 README 三處仍寫「信心程度／低信心／高信心」，與格式檔「已不列信心欄，強弱由依據措辭表達」矛盾。已統一措辭。
- ✅ **🔴 study-planner 範例數字錯**：「師 842 題」實為師+士合計（師=404），與自身「只取本等別數字」規則矛盾。已改 404。
- ✅ **🔴 週期型態處理表缺「偶發」型**：`tags_cycles.json` 實有 15 個 tag 型態=偶發，exam-trend-forecast §1-1 處理表卻無對應列。已補（低優先；動態觸及才上調）。
- ✅ **🟡 版本號不一致**：README badge 0.6.0 vs plugin.json 0.6.1。已統一升 0.6.2。
- ✅ **🟡 `user-config-spec.md` 檔首共用清單錯**：列了未引用本規格的 exam-archive、漏了主要使用者 study-planner。已改正。
- ✅ **🟡 錯字**：exam-tutor 抽考觸發語「來個快範」語意不明，已刪除（「抽考」「快考幾題」已足）。
- ✅ **🟡 README 等待時間說明**：未反映 `/猜題` 兩段式（第一段秒回、僅可選動態需 5–8 分鐘）；週期型態清單漏「新興熱點」。均已補。
- ✅ **🟢 小修**：`exam_date` 明定儲存一律西元 ISO（民國輸入先換算）；exam-tutor 選擇題配分註補「師消防法規測驗部分亦同」；兩處「詳見 docs 設計筆記」改為「設計背景見」（明示演算法以 SKILL 內文為準，與 docs「非 skill 執行時讀取」定位一致）。

> 體檢中驗證無誤（未改）：README「160 卷、3680 題」、圖例「17 類 284 個」、設備條文索引「338 列」、各 skill 輸出檔名與命名慣例、9 個 slash command 對應。

## [0.6.1] - 2026-07-11 — 測試回歸修正（issue #14）

依 issue #14 全功能測試報告修正發現之缺陷（版本 → `0.6.1`）：

- ✅ **🔴 `/申論猜題` 邏輯錯誤**：原指令措辭使其改跑 exam-trend-forecast、只輸出「猜題範圍」報告後叫使用者再呼叫一次自己，形成循環，從未產出預測題目與擬答。已重寫 `skills/申論猜題/SKILL.md` 與 exam-tutor「申論猜題＋擬答模式」開頭：明確直接走 exam-tutor 擬答模式、**禁止改跑 `/猜題`／只輸出猜題範圍就停**；範圍來源改為「指定科目 → session 既有猜題範圍 → 就地讀 tags_summary 高頻考點」依序取用，缺猜題範圍不中止；必產出預測題目＋答題架構＋擬答。
- ✅ **🟢 `/出考卷` 缺【模擬考】標註／偶爾回傳原卷**：回歸測試發現根因為 **exam-tutor 根本沒有「整卷模擬考模式」段落**，指令叫它進入不存在的模式，模型只能即興發揮（漏標記、甚至被「原卷」字樣誤導成回傳考古題原卷、繞過組卷）。已於 `skills/exam-tutor/SKILL.md` 補上正式「整卷模擬考模式」段落：兩條鐵則（首尾 `【模擬考】` 不得省略、**自行命題絕不回傳原卷**，要原卷導向 exam-archive）＋版面依 `試卷格式.md`＋等別分流＋**輸出前自我檢查**；`skills/出考卷/SKILL.md` 同步指向該段落並澄清「原卷版面」指版面格式而非取用原卷內容。
- ✅ **🟢 `/懶人包` 存檔路徑偏差**：`skills/statute-memorizer/SKILL.md` 明示懶人包一律入 `forecasts/`，不可與一般記憶筆記的 `notes/` 混淆。
- ✅ **🟢 猜題檔名民國年不一致**：`reference/user-config-spec.md` 與 exam-trend-forecast 固定「`<民國年>`＝目標考試年度（下一次考試年）」，統一算法（有 `exam_date` 取其年、否則取下一個六月第一個週六所在年），不得用當前系統年。
- ✅ **🟡 `/設定` → 改名 `/備考設定`**：報告中「`/設定` 在 existing_home 穩定失敗、fresh_home 穩定成功」，加上另一次測試回覆「找不到名為『設定』的 skill、被當自然語言解讀」，研判籠統的兩字名稱易與 harness／其他 config 概念混淆而載入不穩。改名為較獨特之 `/備考設定`（資料夾 `skills/備考設定/`），並同步更新 README、`user-config-spec.md`、exam-tutor 之引用；同時補上原缺少的 `argument-hint`（八個指令 skill 一致）。註：environment-dependent 之不穩定根因仍在 harness 層，改名為降低誤判之嘗試，非保證根治。

> 未處理（設計取捨，非缺陷）：`/猜題`、`/申論猜題` 的動態檢索耗時 5–8 分鐘屬必要重工作，建議以文件／UX 告知預期等待時間，已留待後續。

## [0.5.0] - 2026-07-06 — README 精簡與功能刪減

- **README 精簡專業化**：改為 功能表格＋指令表（含範例欄）＋緊湊使用範例＋安裝＋目錄結構＋設計原則；題庫現況與資料準備等維護者內容移至 `docs/資料維護.md`。
- **刪除 /猜題清單**：連同 study-planner 之「猜題清單管理」機制一併移除（猜題範圍的追蹤改由 /讀書計畫 承接：高信心考點於考前衝刺週優先排入）。
- **刪除 /記憶重點** 指令：記憶卡／口訣仍為 statute-memorizer 之能力（自然語言觸發），僅移除獨立指令；讀書計畫建議行動改指 /對照表。
- **/懶人包 加入平時弱點**：來源合成改為 猜題範圍＋平時弱點（weak_tally，標注「⚠ 你的弱點」且排序優先）＋內建對照表。
- plugin.json 0.5.0（指令 8 個）。
- **批改配分改實際制度（2026-07-06 補）**：申論題每題 0–25 分、選擇題每題 1.25 分（士測驗 40 題共 50 分），取代原 0–100 分制；README 使用範例改按五個 skill 分節、全部以 code block 問答呈現（每 skill ≥2 例）。

## [0.4.0] - 2026-07-06 — 猜題功能職責重構

使用者裁示：exam-trend-forecast **只負責猜題**，產出「猜題範圍」（範圍＋依據＋信心，例：瓦斯氣爆→瓦斯漏氣火警自動警報設備設置規定；施工中火災→消防法施工中消防防護計畫），四種衍生產出拆分至各自職責的 skill：

- **申論猜題＋擬答 → exam-tutor**（新增「申論猜題＋擬答模式」，沿用擬答法源硬性規則）
- **可勾銷猜題清單 → study-planner**（新增「猜題清單管理」節，跨對話接續；新增 /猜題清單 指令）
- **考前必背懶人包 → statute-memorizer**（新增懶人包產出形式）
- **直接練 → exam-tutor**（原有交棒機制不變）

exam-trend-forecast 報告尾僅指路不代為執行；/猜題、/申論猜題、/懶人包 指令已改指向新東家；README 功能描述與使用範例同步更新；plugin.json 0.4.0。

## [0.3.0] - 2026-07-06 — README 範例、repo 體檢、讀書計畫、擬答法源

- **README**：新增「使用範例」章（12 項功能各附對話範例）；題庫現況更正為 **3680 題／160 卷**、索引產生器更正為 `rebuild_index.py`＋`analyze_corpus.py`（原誤載已棄用之 build_tags.py）；安裝 URL 舊 repo 名 `fire-safety-engineer-tutor` → `fire-safety-personnel-tutor`；目錄結構補 scripts/、docs/。
- **plugin.json**：repository 欄位同步改正舊 repo 名；version 0.3.0。
- **corpus/INGEST.md**：修正「士卷與師同為申論+測驗」之矛盾語句（師為全申論）。
- **scripts/README.md**：新增，逐支說明 9 支腳本用途與 build_tags.py 之 DEPRECATED 保留原因。
- **study-planner 新 skill＋/讀書計畫**：以設備為主體、按等別命題頻率排單元（設置標準/檢修基準依設備條文索引切單元），順序清單預設、提供考試日期＋每週時數時產逐週排程（考前預留總複習與猜題）；計畫檔存 `<data_dir>/plans/`；exam-tutor 選擇設備優先依計畫提議。config.json 增列選填 `exam_date`、`weekly_hours`。
- **擬答法源硬性規則**：exam-trend-forecast 申論擬答與 exam-tutor 批改正解，每一論點均須附法源依據（法規名＋條號＋版本日期）；無法源之推論標注「非法源：實務見解／計算推導」。
- 申論猜題與 /猜題 之關係確認：保留兩入口（/猜題 產出選單之一＋/申論猜題 捷徑）。

## [0.2.0] - 2026-07-06 — 「熟悉法規」與「考前猜題」功能

依使用者訪談需求（零碎抽考、弱點複習、記憶重點、跨法規對照、猜題四產出、等別與記錄模式初次詢問）完成：

- **`reference/` 內建資產**：`設備條文索引.md`（自使用者試算表 PDF 轉寫 338 列，法源/條文/場所/設備分類/項目；exam-tutor 連續出題課綱地圖）＋ 8 張高頻對照表（檢修申報、場所分類門檻、水系統數值、警報距離時限、化學藥劑放射、避難門檻收容、消防法罰則、公危管制量；均自 statutes 逐條擷取並抽查核對）＋ `user-config-spec.md`（config.json／progress.json 規格單一來源）＋ `index.md`。
- **exam-tutor 擴充**：設備選擇主動提議（科目/弱點/頻率/覆蓋進度綜合判斷）、以設備條文索引為課綱連續出題（一般場所→公危→檢修→延伸知識）、整合性出題（「項目」欄）、出題鐵則（改編必驗證、答案須在法規找得到）、快速抽考模式（3~5 題）、弱點複習模式、學習進度改依 user-config-spec。
- **statute-memorizer 新 skill**：記憶卡／口訣／易混淆組／跨法規對照表，內建優先＋即時生成。
- **exam-trend-forecast 擴充**：應考等別影響猜題比重；四種後續產出（直接練交棒 exam-tutor、可勾銷猜題清單（跨對話接續）、申論猜題＋擬答、考前必背懶人包）；輸出檔命名慣例。
- **8 個中文 slash commands**（2026-07 依官方文件遷移至 `skills/<名稱>/SKILL.md`，`disable-model-invocation: true`）：/抽考、/弱點複習、/對照表、/猜題、/懶人包、/申論猜題、/備考設定（原 /設定，v0.6.1 改名）、/讀書計畫。
- plugin.json 升版 0.2.0。

---

# 未隨版本發布的資料與內容工作

> 下列為未單獨升版的資料整理、索引建置與內容校核；其成果隨後續版本一併釋出。

### 發布層與規格體檢（原 0.9.0）

發布層與 plugin 規格的體檢修正。**本版把 0.8.0 之後累積、但因未 bump 版本而未送達使用者的全部資料校正一併釋出**——主要是 `statutes/` 773 個檔案的品質修正（公式改 LaTeX、`²` 上標、階層編號拆行、編號括號全形、附檔名小寫、附圖歸位）。

### 新增

- 五個核心 skill 與十個 slash command skill 全面宣告 `allowed-tools`：查標籤索引（`jq`）、判讀原卷附圖（`pdftoppm`）與唯讀檔案存取不再反覆跳權限提示。**寫入使用者資料仍逐次徵詢同意**（`Write`／`Edit` 刻意不預先授權）。
- `jq` 未安裝時的降級路徑：各 skill 的「優雅退場」表新增以 `python3 -c` 取單鍵的等效作法，維持「只取所需單鍵、勿整檔載入」的鐵則；`pdftoppm` 缺席時改以文字描述附圖並附原卷頁碼，不杜撰圖形內容。
- CI（`.github/workflows/validate.yml`）：manifest 與版本一致性、skill frontmatter、`corpus/index.json` 160 卷路徑、960 條文件內部連結、上標與條文編號全形括號規範、法規版本時效，外加官方 `claude plugin validate --strict`。檢查邏輯在 `scripts/ci_check_repo.py`，可本機執行。
- `scripts/requirements.txt`：宣告 PyMuPDF／Pillow／numpy 與系統層 poppler-utils（**維護者專用，plugin 使用者不需安裝**）。
- README 新增「前置需求」與「安裝體積」說明（工作樹 142 MB，含 git 歷史首次下載約 265 MB）。
- `plugin.json`／`marketplace.json` 補 `keywords`、`category`、授權與作者資訊。

### 修正

- README 目錄結構誤植 slash command 數量（九 → 十）。
- README 授權說明改為**分離敘述**：程式碼與整理成果為 MIT；`corpus/`、`statutes/` 之官方原始資料不在 MIT 標的內，各依原出處條款。
- 維護者指令檔自 repo 根目錄移至 `.claude/CLAUDE.md`。兩個位置對 Claude Code 而言等價（同樣載入為專案脈絡），但放在 **plugin 根目錄**會被官方 `claude plugin validate` 判為無效內容（plugin 不透過 CLAUDE.md 提供脈絡，應用 skill）。移動後 `--strict` 驗證通過，維護者在本 repo 的開發脈絡不受影響。檔首並補上適用範圍說明。

## statutes 第一層品質修正（2026-07-20，issue #47／PR #48）

對 `statutes/` 第一層 39 個法規 md 之品質修正（公式 LaTeX、上標、編號拆行、全形括號、附件缺漏註記等），詳見 issue #47 與 PR #48。依 review 指示，**轉寫更正與待核對疑點統一記錄於本檔**，法條檔內只保留轉錄之法條文字。

### 轉寫更正紀錄（依上下文可確定之 OCR 誤植；未能連線官方網站逐字核對，仍以官方現行版為準）

- **`1_11_公共危險物品試驗方法及判定基準.md`**（整檔重排：黏連段落依編號層級拆行、半形編號括號改全形、`cm2/cm3/mm3`／`㎜` 合字改上標與標準寫法）：
  - 第三點「固體」之編號原誤植為「(一)」，依前後文（其定義引用「（一）液體」）更正為「（二）」。
  - 第四點（五）1（5）判定基準第二級之「2.4-二硝基甲苯」更正為「2,4-二硝基甲苯」（化學命名，同段前文均作 2,4-）。
  - 第四點（四）表中「Pensky-Mart-ens」（斷字殘留）更正為「Pensky-Martens」；「ASTMD3278」補空格為「ASTM D3278」；「ISO 3679、3680ASTM D92」補頓號為「ISO 3679、3680、ASTM D92」。
- **`1_10_…設置標準暨安全管理辦法.md` 第 63 條**：變數說明「比重」一項原誤植為大寫「W」（與儲存能力重複），依第二款計算式 $W = C_1 \times w \times V_2$ 更正為小寫「w」；「Mpa」更正為「MPa」。
- **`1_13_可燃性高壓氣體儲存場所防爆牆設置基準.md`** 鋼筋搭接長度表：欄名「固鋼・有鉤」依鋼筋工程慣用語（異形鋼筋／圓鋼之別）更正為「圓鋼・有鉤」。
- **`1_14_可燃性高壓氣體儲存場所防護牆設置基準.md`** 第二點表：「鋼板製B」高度欄原作「2000m 以上」，依同表其他欄位單位判斷為 OCR 誤植，更正為「2000 mm 以上」。
- **`2_04_複合用途建築物判斷基準.md`**：移除 30 個 OCR 殘留之隱形私用區字元（U+E686–U+E690，原置於表格內（一）（二）等編號前）。

### 待核對疑點（無法自本地判定，檔內保留原樣，請對照官方全文）

- `1_13` 鋼筋搭接長度表「接頭｜固定」兩欄疑為原表多層表頭之攤平結果，結構待與原始檔核對。
- ~~`1_11` 之各項疑點~~：已於 2026-07-20 與官方全文逐段比對結案，見下節。

### 1_11 官方全文逐段比對結果（2026-07-20）

owner 提供官方全文（消防法令查詢系統行動版列印 PDF，修正日期 102/03/07、發布文號內授消字第1020821155號、網站法規資料更新日期 115/06/30；已入庫 `statutes/原始檔案/公共危險物品試驗方法及判定基準/`），與 `1_11` md 逐段比對：

- **全檔數值相符**：篩網孔徑（1.18mm、300／150／250／500／180／53μm）、試料重量（30g、100mg、500g、2g、15g、5g 等）、器具尺寸、溫濕度與風速、堆積比（1 比 1.75、1 比 2）、混合比（1 比 1、3 比 1）、耐壓（50kgf/cm²、6kgf/cm²）、加熱器（1,000℃、700W）、判定門檻（200 l/kg-hr、閃火點各級距）均與官方一致，未發現 OCR 數值誤差。
- **原待核對疑點結案**：
  - 「克里排開放式」：官方原文即作「克里排開放式」，非轉換誤植，維持原樣。
  - 第四點（四）2（2）「閃火點＜100℃且自燃溫度＜100℃」：與官方原文一致，無辨識誤差。
  - 第四點（六）1（5）「試驗物品依照的方式量測燃燒時間」：官方原文「依照」之後為一無法顯示之字元（列印呈「□」，應係引用前述步驟之符號），非 md 轉換漏字；md 依可讀文字轉錄，維持現狀。
- **官方原文本身之誤植（md 之邏輯更正維持）**：第三點固體之編號官方原文亦誤植作「（一）」（md 更正為「（二）」）；第四點（五）判定基準第二級官方原文亦作「2.4-二硝基甲苯」（md 更正為「2,4-」）。
- **無法核對處**：第四點（四）閃火點測試儀表之 Pensky-Martens 列於官方列印時遭網頁頁首遮蔽，該列維持原轉換內容；第四點（五）「依據試驗步驟求得」與「綜合、數據做圖」二句中，官方原文各有無法顯示字元（□，係引用前述步驟之符號），md 依可讀文字轉錄。

### 表格儲存格多項目處理決策（issue #47 C-4，經 PR review 定案）

初版依 owner 指示採「一項一列拆開」，review 後改為**維持原列結構、儲存格內以 `<br>` 分行**（適用 `1_10` 附表一、`2_01` 收容人員計算表）；儲存格外之黏連段落仍改 `-` 清單。CLAUDE.md「附表處理原則」第 5 點同步修訂為「得以 `<br>` 分行，若不清楚停下詢問使用者」。

### 標示規格嵌圖紀錄（issue #47 F 項）

- **`2_05` 二氧化碳**：owner 提供原始 DOC（台（八五）內消字第8577247號）後入庫 `statutes/原始檔案/`；自 DOC Data stream 抽出內嵌之消防署官網圖片快取（`fl-016.gif`、`fl-017.gif` 之 PNG）嵌入 md，取代原 ASCII 框線圖示；牌面文字與原圖放大核對一致（含（四）之「放射前警鈴響時」）。
- **`2_06` 乾粉**：無原始檔，依 owner 指示以 `2_05` 原始圖版式改製（「二氧化碳」字樣改「乾粉」，（四）牌面依原轉錄作「室內設有乾粉式滅火設備」），**非官方原圖**；取得官方原始檔後應更換。

## statutes 子資料夾品質修正（2026-07-20，承 issue #47 標準）

依 issue #47／PR #48 之同一標準，檢視 `statutes/` 三個子資料夾共 72 個 md 檔（認可基準、消防安全設備及必要檢修項目檢修基準、消防安全設備測試報告書測試方法及判定要領），修正項目：

- **合字清理**：`㎜/㎝/㎏` 改 `mm/cm/kg`（14 檔）；`kgf/㎝2` 等併正為 `kgf/cm²`。`ℓ`（公升）與 `KCℓ` 為官方慣用寫法、非合字，保留。
- **編號括號全形**：半形 `(一)`～`(十)`、`(1)`、`(A)`、`(a)` 等一律改全形括號（內部英數維持半形，如 `（A）`、`（1）`）；檢修基準全資料夾共約 980 處。規格／單位標示 `dB(A)`、`(1W)`、圖號 `圖 1(a)`、化學相態 `CO₂(g)` 非編號，維持原樣。
- **黏連編號拆行**：`4_04`／`4_11`／`4_12` 之同行多編號（（一）（二）…）逐項拆行；檢修基準各章 `A.標題(A)內容` 式黏連約 25 處拆行；`第13章` 空氣管式試驗多層黏連（（I）～（IV）、流通／接點水高試驗）依原始 PDF 重排；`4_01` 試驗片 `(a)(b)(c)`、測試報告書 `第01章` `(a)(b)(c)` 拆為子清單；`第15章` 表 15-1 設置場所儲存格外黏連改 `-` 清單。
- **段尾黏標題拆行**：檢修基準各章之無編號子標題（水源、電動機之控制裝置、啟動裝置等）遭黏於前段句尾者，經與原始 PDF 逐一核對該標題確為獨立行後拆出，共 178 處（19 章）；`第11章` 另有 39 處 `。判定方法／注意事項／檢查方法` 內嵌合併與 19 處 `N、標題檢查方法` 黏連，依原始 PDF 全數重排。
- **公式 LaTeX 化**：`第25章` 放射量 $Q = K\sqrt{P}$（原全形 `Ｑ＝Ｋ√𝑃`）比照第 02 章格式；`第07章` 25％還原時間之兩處算例（180÷4、還原量內插比）轉 LaTeX。`4_08` 等缺點判定摘要之「＝嚴重缺點」為分類簡寫、非數學公式，維持。
- **全形英數與隱形字元**：全形拉丁字母（`之Ａ規定`、`ｖ型皮帶`、`Ｍ 10×1.5`、`六○○Ｖ`、`Ｌ／Ｍ／Ｓ級` 等 31 處）改半形；移除 OCR 殘留私用區字元 `第07章` U+F06C×4（原為備註項圓點）、`第13章` U+F0AD×2。
- **其他**：`4_06` 半形括號包中英文（（dB）、（Anechoic room）、（附圖 N）等）改全形、修復「可吸／收」斷行黏字；`第20章`／`第22章` 注意事項黏連拆行。

**已檢視未改**：檢修基準 `第11章` 部分區段之 PDF 三層編號（A./(A)/a.）於原轉換即扁平化為「無前綴標題＋（Ａ）項次」，本次僅拆行未回復原層級，如需完整層級請對照 `statutes/原始檔案/` 原始 PDF；認可基準表格儲存格內 `1. 2.` 多項目（如 `4_08` 試驗設備表）依附表處理原則屬「得以 `<br>` 分行」之選擇項，維持現狀。

## 猜題週期統計改按科目獨立（2026-07-15，PR #25）

方法論修正：**每一科命題委員不同，週期／頻率統計一律按科目（卷別）獨立分析，不跨科、不跨等別合併或推論**（未升版）：

- ✅ **`analyze_cycles.py` 改以（等別, 科目, 維度, tag）為鍵**：`tags_cycles.json` 結構改 `{等別:{科目:{維度:{tag}}}}`（師 6 科／士 4 科）；`命題週期分析.md` 改「各等別 → 各科目」分節。已重生。
- ✅ **新增「退役常年型」型態**：該科歷史常年型但近 ≥2 屆缺席 → 高信心回鍋候選（例：師避難科 §188）；入型態表與分類邏輯。
- ✅ **型態須配題型**：`猜題報告格式.md` 加註「頻率高但本卷非其常出題型者須降優先」。
- ✅ **同科叢集**：同科相鄰／相關條文同進同退時整群評估回鍋（跨科不算）。
- ✅ 撤回違反各科獨立原則之觀察（系統級漂移、跨等別領先、跨卷題型反轉），不實作。

## 命題週期型態統計（2026-07-07）

使用者需求：猜題除時事外，也要找出「過去沒考或少考」的部分——有的考點每屆都考只換說法、有的考過一次不再出現、有的考後三年內不會再考、有的每 N 年回鍋。

- **`scripts/analyze_cycles.py`**：由 tags_index 之題目參照（等別/年/科目）計算各考點（by_article/by_equipment/by_topic）依等別之逐年序列，分類出題型態：**常年型**（出現率≥50%）／**新興熱點**（近年連考，常見於新修法）／**週期到期**（距今≥平均再現間隔——回鍋候選）／**冷卻中**（剛考過）／**一次性**／偶發；另比對設備條文索引產出**未考缺口**清單（附「條號標籤非全覆蓋」判讀警語）。
- 產物：`corpus/tags_cycles.json`（48KB，可整檔載入，含逐年序列/平均間隔/距今/主要考科）＋`corpus/命題週期分析.md`（人可讀報告）。
- **exam-trend-forecast 整合**：新增「1-1 週期型態維度」節（六型態之猜題處理、與動態維度合成順序：頻率→週期→修法時事，動態觸及可推翻冷卻降權）；輸出類別增為四類（＋**回鍋與缺口**），每項猜題範圍須附出題型態與數據。
- 標準流程更新：rebuild_index → analyze_corpus → **analyze_cycles**（scripts/README、docs/資料維護）。
- 實測範例：士類消防法 §26 平均每 4 年一考、已 10 年未考（週期到期）；§13 平均 2.6 年一考、115 剛考（冷卻中）；設置標準 §12 平均 1.83 年、已 4 年未考（週期到期）。

## 申論擬答「逐步推導」規範（2026-07-07）

使用者需求：申論擬答須詳盡如數學考試——直接寫答案分數不高，必須寫出推論過程，且每一步盡可能有法律依據。

- **exam-tutor 申論猜題＋擬答模式**新增「逐步推導規範」：每段論述依「要件 → 法源 → 涵攝 → 小結」推進（定義引用語出處 → 條文關鍵句 → 題目事實對應條文要件 → 小結）；計算題每步公式標明法源、代入值說明來源、附單位逐步演算、結果檢核，不得跳步。
- **批改標準同步**：申論題「推論過程是給分主體」——只寫結論未推導者比照實際閱卷給低分並指出缺失之過程分；示範正解依同規範完整寫出。
- /申論猜題 指令、README 申論批改範例（改為 14/25 分含「過程分未拿到」講評與涵攝示範）同步更新。

## statutes ASCII 框線表格轉換（2026-07-06）

全部**資料表格**已轉為 markdown 表：

- `2_01`（17 處）：§66、§73、§99、§118、§120、§121×2、§122、§146-1、§146-2×2、§157、§160、§213×4。合併儲存格逐列展開並加「轉寫註」；§99（$Q=X-Y\cdot a/A$）、§146-2（$D=kh$）、§214（$N=Aq/C$）之純文字公式一併轉 LaTeX；§121「他建築物」轉檔漏字補正。
- `1_10`（9 表）：§21、§25、§30×3、§39、§66、§67×2；§66/§67 安全距離之 **ASCII 根號**（`0.12┘x+10000` 等）轉為 LaTeX（$0.12\sqrt{x+10000}$ 等）。
- `1_13`（2 表）、`1_09`（1 表）、`3_02`（4 表，§88 內部裝修材料複雜合併矩陣已逐列攤平）、`3_03`（1 表，並補「供**教**室使用」OCR 漏字）。
- **`2_05`／`2_06`（各 4 處）不轉換**：其框線為**標示牌外觀圖示**（標示牌實體版面＋A/B 尺寸標註），非資料表格，已於兩檔檔首加註說明依附表處理原則保留原樣。

（轉換抽查之建議後續見 [`待辦與優化建議.md`](docs/待辦與優化建議.md)。）

## 題目內容品質校核（2026-07-04，issue #3）

> 2026-07-04 依 issue #3 完成 ①圖形題、②更正答案、③兩欄排版選項順序全面比對。

### ① 圖形題（`圖形題` flag，共 11 題；原記「4 題」已過時）——已處理

- **含圖 7 題已內嵌圖片**（`scripts/extract_figures.py` 自原卷 PDF 裁切，PNG 輸出至引用之 md 同資料夾 `corpus/md/<類>/<年>/`，逐張人工核對）：
士/100/0910 Q7、Q8（選項圖例各 4 張）；士/104/0707 Q34（危害圖示 4 張）；士/111/0809 Q35（音域圖 4 張）；師/114/0801 甲2；師/115/0801 甲2、甲4（題幹示意圖各 1 張）。md 已可直接顯示圖片，無「僅佔位」問題。
- **試繪型 4 題原卷即無圖，無需擷取**：師/100/0904 甲3、師/102/0701 甲2、師/103/0701 甲4、師/113/0801 甲2（題目要求考生自行繪圖；`圖形題` flag 於此表「作答需繪圖」，非「題幹含圖」）。
- 附帶修正：師/115/0801 甲4 題幹「流量係數皆為 C」依原卷更正為「皆為 α」。

### ② 更正答案（`更正答案` flag，共 14 題）——已二次核對

以官方 `_答案.pdf`／`_更正答案.pdf`（文字層座標配對＋人工目視）逐題核對，並**全卷比對該 10 卷共 400 題答案**：

- 14 題更正註記與更正答案卷**全數相符**。
- 全卷比對另揪出並修正 md 誤植 5 筆：士/109/0907 Q7 答案 (D)→(B)；士/110/0809 Q28 (B)→(A)、Q30 (B)→(A)；士/112/0810 Q38 標準答案改記原公告 (C)（更正後 A/C/AC 均給分不變）；士/113/0809 Q11 註記「原公告答案為 (A)」→ (C)。

### ③ 兩欄排版選項順序——已全面比對（2026-07-04）

以 `scripts/check_option_order.py` 對全部選擇題卷（士 64 卷＋師法規 16 卷，共 3,200 題）進行 md 選項順序 vs 原卷 PDF 文件順序之全面比對：

- **0 題錯置**：3,189 題自動驗證一致；4 題圖形選項題（見 ①）不適用；7 題因數學符號（√、^、上下標、≧）PDF 抽取瑕疵無法自動配對，已逐題人工目視原卷確認順序無誤（清單見腳本檔首）。
- 既有修正（士/101/0907 Q40）經驗證現與原卷一致。
- 附帶發現並修正 1 筆文字誤植：師/108/0902 Q20 選項 (B)「設置**近**端啟動裝置」（原卷如此，係作為錯誤選項設計；md 誤植為「遠端」會使該選項依設置標準反而成立，扭曲題意）。
- 因無錯置題，原提議之 `選項順序疑慮` 旗標無需新增。

## statutes 內容缺口（已完成部分）

> 各法規檔首均已附「📌 免責聲明」，引用前以官方公告現行版為準。以下為已完成之補強；仍待補強處見 [`待辦與優化建議.md`](docs/待辦與優化建議.md)。

- ✅ **公式對照官方 PDF**（2026-07-04）：§83-2、§97-3、§133 之公式與附表已依使用者提供之單條 PDF 逐項核對，均相符；§133 原文快照段之 α 誤植與轉換殘留字元一併修正。
- ✅ 設置標準 §84（2026-07-04）：使用者提供單條 PDF 已入庫（`原始檔案/各類場所消防安全設備設置標準/第 84 條完整條文.pdf`），二氧化碳放射時間表已內嵌 md 並核對相符。
- ✅ **版本日期已補（2026-07-06 網路查證）**：水道連結型自動撒水設備設置基準＝107-11-29 訂定發布（內授消字第1070824140號）；測試報告書要領最新修正＝109-06-05（第 11 章之 1），前次 108-08-20（第 2、4-1、9、11-1、22 章）——已寫入各該 md 檔首，仍以官方現行公告為準。

## 標籤分類校正（方針確立）

- ✅ `scripts/build_tags.py` **已停用（DEPRECATED）**：執行時直接報錯擋下（其關鍵字結果會覆蓋語意標籤）；僅保留 EQUIP／LAWS／TOPICS 詞彙供 `rebuild_index.py` 匯入。
- ✅ 緊急廣播設備＝**警報系統**；by_system 已改為設備師 4 系統考科（水／化學／警報／避難系統）；師設備全申論卷已補標並語意精修（僅少數純化學原理題依規則不標法規）。

（校正方針與仍在進行的語意校正見 [`待辦與優化建議.md`](docs/待辦與優化建議.md)。）

## Skills 整合查詢入口（2026-06-21）

`exam-tutor`、`exam-trend-forecast`、`exam-archive` 之 SKILL.md 已載明以標籤索引為查詢入口：

- `corpus/tags_summary.json`（精簡計數＋逐年，可整檔載入）→ 頻率／趨勢統計。
- `corpus/tags_index.json`（tag→題目參照）→ **勿整檔載入**，用 `jq` 取單鍵（如 `jq '.by_equipment["自動撒水設備"]' …`）。
- `corpus/命題頻率分析.md` → 人可讀報告。
- exam-tutor 出題頻率改讀 tags_summary（近五年）、抽題與複習以 by_system/by_equipment 單鍵；exam-trend-forecast 歷史底改讀 tags_summary 之 by_year；exam-archive 新增「依標籤查題」。

## `tags_index.json` 載入優化（2026-06-21）

- 新增 `corpus/tags_summary.json`（約 47 KB，僅各 tag 之總數／師士／逐年計數，**無題目參照陣列**）供 skill 整檔載入做統計，取代載入完整 `tags_index.json`（約 370 KB）。
- 載入策略寫入各 SKILL：總覽用 summary；取單一 tag 之題目用 `jq` 取單鍵；取題目內容由參照推檔路徑讀 md。由 `scripts/analyze_corpus.py` 產生。

## 年度難易度與命題頻率分析（2026-06-21）

- `scripts/analyze_corpus.py` 由 `tags_index.json` 計算各維度總數、師/士、逐年次數，並產出 `corpus/命題頻率分析.md`（系統／設備／法規／條號／火災學主題之頻率；題型、計算題、找錯誤vs找正確、系統別之逐年趨勢）。
- 後續可由 exam-trend-forecast 直接取用。

## 雜項

- ✅（2026-07-04）撒水頭附件資料夾已改名 `原始檔案/撒水頭認可基準/`（與 md `4_08_撒水頭認可基準.md` 對齊；內部 PDF 保留官方文件名 `密閉式撒水頭認可基準.pdf`，md 連結已同步更新）。

---

---

# 測試計畫與驗收紀錄

> 本節為**索引與經驗萃取**：三輪驗收測試的計畫與逐項證據都留在 GitHub issue 留言（repo 內不另存測試計畫檔），此處只記「測了什麼、發現什麼、下次該怎麼測」。細節一律以各該 issue 為準。

## 三輪一覽

| 輪次 | issue | 對象 | 驗證方式 | 結果 | 衍生 |
|---|---|---|---|---|---|
| 第一輪 | [#14](https://github.com/yyc1217/fire-safety-personnel-tutor/issues/14) | v0.6.0 → 0.6.1 | 真實安裝互動實測（`fresh_home`／`existing_home` 兩種狀態各跑一輪） | 60+ 項通過、5 項缺陷、3 項未測 | PR #17、[#18](https://github.com/yyc1217/fire-safety-personnel-tutor/issues/18) |
| 第二輪 | [#27](https://github.com/yyc1217/fire-safety-personnel-tutor/issues/27) | PR #21–#26（v0.6.1 → 0.8.0） | 前輪：資料檢查🔧／靜態一致性📐／模擬執行🧪；後輪：subagent 實跑🤖 | 前輪 32 項通過＋6 項待互動；後輪 6 項全過，另發現 **20 個問題**（🔴4／🟠8／🟡8） | PR #28、issue #29–#34 |
| 第三輪 | [#59](https://github.com/yyc1217/fire-safety-personnel-tutor/issues/59) | v0.10.0（PR #58） | 靜態核對 ＋ headless 實際安裝實跑（`claude -p --output-format stream-json`） | 32 項已測、31 通過（唯一不通過之 D-1② 已修） | `d8280bf`／`59cc24f`／`cb9db06`／`1817e5f`／`2b6ae59` |

## 各輪最有價值的發現

- **#14（全功能回歸）**：`/申論猜題` 措辭使其改跑猜題、只輸出範圍就叫使用者再呼叫自己，**形成循環、從未產出擬答**；`/出考卷` 指向 exam-tutor 中**根本不存在**的「整卷模擬考模式」段落，模型只能即興發揮（漏標記、甚至回傳原卷）。教訓：**指令指向的東西必須真的存在**。另 `/設定` 因名稱過於籠統而載入不穩，改名 `/備考設定`。
- **#27（規格層體檢）**：三個 🔴 都會**產生錯誤內容給考生且無從察覺**——條號標籤與現行 `statutes/` 無映射層（#29，112 年重編後題目與條文對不上）、corpus 單筆錯標（#30）、檢修基準快照過期 18 個月（#31）。共同特徵是**兩邊都「有出處」**，比杜撰更難發現。另 #32／#33／#34 分別歸類為「規則不可實施」「範本自我矛盾」「規格缺口」三型。本輪並**推翻自己前一輪已勾選的一項**（`by_equipment` 與設備條文索引僅 25/30 對齊），因該項當初以合成資料模擬、未踩到真實對映。
- **#59（harness 層）**：**10 個中文斜線指令全部未註冊**（非 ASCII 目錄名 ＋ frontmatter 缺 `name:`），README 宣傳的整組指令等於失效；靜態檢查與 `claude plugin validate` 全綠，只有實跑才會發現，且它使**先前所有「用 `/抽考` 測」的結論失效**（模型是在指令失敗後自行退回叫用 `exam-tutor`）。其餘三項機制性發現：`${user_config.*}` **只在被叫用的那份 SKILL.md 內文代入**（`/出考卷` 因 fork 無主對話可兜底，會靜默出錯等別的卷）；`allowed-tools` 的授權**只涵蓋叫用的那一個回合**；權限提示殘留的真因是 `additionalDirectories` 未涵蓋 plugin 安裝目錄，而非 `allow` 指令清單不足。

## 訂下一份測試計畫時可直接沿用的教訓

1. **靜態一致性與模擬執行測不到 harness 層**——指令註冊、`${CLAUDE_PLUGIN_ROOT}` 解析、`context: fork`、設定代入、權限提示，這層只有實際安裝跑得到，而它正是最容易整組全滅的一層。
2. **判定以工具呼叫紀錄為準**（有沒有 Read 模式檔、有沒有 Write），不以輸出讀起來像不像為準。規範沒被讀到時行為會「看起來還是對的，但規範會漂」。
3. **規則若在資料上不可實施，模型不會報錯，會自行發明補法**（自創 `points` 欄、自行補一整節火災學、自創 `q_id` 格式）。單次執行完全正常，跨 session 才對不上。測法：**同一份輸入跑兩次獨立 session 比對產出**。
4. **測降級路徑要把退路堵死**：模型繞過缺工具的能力遠超預期（`pdftoppm` 缺席就改用 `pdftocairo`、再擋就自己寫 PDF parser 抽圖、還有 corpus 預抽 PNG 可讀）。沒堵死時測到的是繞道，不是降級。
5. **headless 測得到的比想像多，但有兩個限制**：`--permission-mode default` ＋ `result.permission_denials` 可精確判定「哪些呼叫需授權」；`num_turns`／`usage` 歸零而 `modelUsage` 龐大可反證 fork 成立。限制是**斜線指令展開不穩定**（同一指令有時展開、有時落到 `Skill` 工具，而經 `Skill` 叫用時 `context: fork` 不生效），且無互動核准（設定對話框等仍須手測）。
6. **測試腳本的前置資料會污染前提**（#59 C-5 無條件先建了讀書計畫，使「該問科目」的前提不成立，測試無效）。每個測項跑前先確認自己的前提成立。
7. **每項都是單次取樣**，通過只代表「這一次做對了」，不是統計結論。
8. **誤判要當場更正並留痕**（#59 有 5 條自我更正、#27 推翻自己一項勾選）。測試報告的可信度取決於錯誤有沒有被講出來；也因此，模型在測試中下的結論不可逕行採信，須以獨立命令複核。

## 尚未結案

- **#59 仍 OPEN**：E-4（合併後確認使用者端 `claude plugin update` 真的收到 0.10.0）、`allowed-tools: Bash(jq *)` 在互動模式是否生效（headless 下疑似未套用）待覆核。
- #14 遺留的 harness 管線層抽驗、#27 標記「建議日常使用中抽驗」之項目。

---

---

# 決策紀錄（已決議不做）

> 經評估後決議不實作／不補之項目，留存備查。

## 題目存取效能：`corpus/questions.json` 平坦索引 — ❌ 不做

經評估：以現行電腦效能，由參照推出檔案路徑後再掃描單一 md 找題已足夠快，毋需另建平坦索引。**不實作**。

## statutes 缺原始檔待補之附表 — ❌ 不補

- ❌ 消防機關辦理建築物消防安全設備審查及查驗作業基準：附件二、四、七、八、十一——不補（2026-07-04 使用者裁示：不重要）。
- ❌ 緊急電源容量計算基準：附錄一～六、附表 1～4、圖 1～4——不補（2026-07-04 使用者裁示：不重要）。
- ❌ 原有合法建築物公共安全改善辦法：附表一、二——不補（2026-07-04 使用者裁示：不重要）。

---

---

# 完成里程碑

- **2026-06-19**：命題大綱所列法規全數入庫並編號（認可基準置於 `statutes/4_認可基準/`）；全題標籤（新增 `by_article` 條號維度）；核心法律「上傳版 vs 記憶版」比對；認可基準依截圖逐項核對更正；所有法規檔首加「📌 免責聲明」。
- **2026-06-21**：逐題語意重標完成（法規維度對應命題大綱標準名、火災學改教材目錄主題、緊急廣播歸警報、`by_system` 改為設備師 4 系統考科、師設備全申論卷補標與精修）；`build_tags.py` 停用、改以 `rebuild_index.py`＋`analyze_corpus.py`；Skills 整合標籤索引查詢入口；新增頻率分析報告與精簡 summary。
- **2026-07-04**（issue #3）：圖形題 7 題圖片自原卷擷取內嵌（PNG 與 md 同資料夾，4 題試繪型原卷無圖）；更正答案 14 題以官方答案卷核對相符、另修正 5 筆答案/註記誤植；選項順序 3,200 題全面比對零錯置、修正師/108/0902 Q20「近端」誤植；新增 `extract_figures.py`、`check_option_order.py`。

---

*最後更新：2026-07-29*
