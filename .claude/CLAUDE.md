# Claude Code 專案指令

> ⚠️ **適用範圍：僅限本 repo 的開發與資料維護作業。**
> 本檔是**維護者**在本 repo 內工作時的規範（法規入庫、格式校正、標籤維護），對安裝本 plugin 的**使用者不生效**。
> 面向使用者的行為一律定義在 `skills/*/SKILL.md` 與 `reference/`；本檔的規範若需影響使用者端輸出，**必須另外寫進對應的 SKILL.md 或 `reference/輸出格式/`**。
>
> **本檔放在 `.claude/CLAUDE.md` 而非 repo 根目錄**：官方對兩個位置一視同仁（皆載入為專案脈絡），但 plugin **根目錄**的 `CLAUDE.md` 會被 `claude plugin validate` 警告為無效內容（plugin 不透過 CLAUDE.md 提供脈絡）。放進 `.claude/` 可同時保有維護者脈絡與乾淨的 plugin 驗證。

## 使用語言

與使用者對話請使用**繁體中文**。

---

## 法規附表附圖驗證提醒（Standing Instruction）

每當使用者上傳新法規並完成入庫後，**必須主動提醒使用者**：

> 請確認這些表格與圖片是否有正確被轉換，無論是否有被轉換成功。

提醒時機：使用者表示「全部法律已上傳完畢」或明確結束某批法規上傳作業時。

提醒內容應包含：
1. 列出本次入庫的法規清單
2. 指出每份法規中有附表或附圖的條文位置
3. 說明哪些表格已成功轉換為 markdown（內嵌）、哪些僅保留連結（含原因）
4. 請使用者自行開啟 `statutes/原始檔案/` 下對應的原始檔，核對內容是否正確

---

## 法條內容與記憶比對提醒（Standing Instruction）

待**全部法規上傳並入庫完畢後**，必須主動執行並向使用者報告：

> 將使用者上傳的每一部法條，與我（模型）記憶中的同一部法條逐一比對，**列出兩者不一樣的地方**。

用途：使用者上傳的是「整理當下的版本快照」，可能因 OCR／轉換誤差、或與訓練資料版本不同而有出入。比對目的在於：
1. 揪出轉換／OCR 可能造成的錯字、數值錯誤、條文遺漏。
2. 標示「上傳版」與「記憶版」差異之處，供使用者判斷何者為現行有效版本（**以官方現行版為準**，記憶可能過時或產生幻覺）。
3. 差異須逐條列出（法規名稱 → 條/點 → 上傳版內容 vs 記憶版內容 → 研判可能原因：版本修正／OCR誤字／記憶不確定）。

注意：模型記憶可能有誤或過時，**比對結果僅供提示**，最終一律以官方公告之現行條文為準；不得以記憶覆寫使用者上傳之內容。

---

## 影像辨識／OCR 內容免責與查證（Standing Instruction）

`statutes/` 內部分法規（尤以認可基準）係由 PDF／影像 OCR、DOC／ODT 擷取後人工整理，各該檔檔首已附「📌 免責聲明」。引用此類內容時：

1. **主動提醒使用者**：本檔部分為影像辨識／OCR 結果，可能有誤；**一切以主管機關（內政部消防署、全國法規資料庫）公告之現行版本為準**。
2. 若使用者對某條文、數值、公式或表格有疑問，或該內容將用於關鍵判斷時，**應主動上網查證**官方現行版本後再回答，不得僅憑本地快照或記憶作答。
3. 發現本地 md 與官方現行版有出入時，以官方為準，並提示使用者更新本地檔。

---

## 附表處理原則（參見 statutes/index.md 完整說明）

1. 可乾淨轉換 → 內嵌 markdown 表格
2. 複雜跨欄表格 → 拆解結構 + 附原始檔連結
3. 圖例、掃描影像 → 僅附連結與說明原因
4. 附件檔名含半形括號 `()` → 一律改全形 `（）`
5. 表格儲存格內有多個編號項目黏連時，**得以** `<br>` 分行（儲存格外之黏連段落改為 `-` 清單）；若不清楚如何處理，停下詢問使用者。

---

## 轉換過程註解一律不寫（Standing Instruction）

`statutes/`、`corpus/` 等 md 檔中，**不得加入描述「轉換過程」的註解**。以下皆屬之，一律不寫，既有者一併刪除：

1. 說明原件版面／合併儲存格如何展開（「原件本表僅有兩個資料列…」「原件之某欄為跨列合併儲存格，markdown 無法合併，故兩列重列相同數值」「非逐列一一對應」）。
2. 說明本庫的呈現選擇（「markdown 無法乾淨轉換，故以截圖保留」「不轉寫為 markdown」「儲存格內之編號項目以 `<br>` 分行」「附表 X 以截圖嵌入、附表 Y 維持內嵌 markdown」）。
3. 說明工作歷程（「依 #XX 使用者裁示改用截圖」「已依使用者上傳截圖核對」「逐格轉寫自原始 PDF 第 X～Y 頁」）。
4. 說明 PDF 版面瑕疵如何判讀（如白色方框疊印、圖標記誤置於條文行）。

**可以保留**者只有兩類：

- **原件內容本身之瑕疵或疑義**，且本檔據以判斷或照錄者。例：「原件（3）印為『但應在 200Hz~3.15Hz 間』，與同句前段之『200Hz 至 6.3kHz』不能相容，研判上限係『3.15kHz』脫漏單位，本檔依此記載。」、「原件無圖 8，圖號自圖 7 跳至圖 9，非本檔漏收。」、原件漏字之補正。
- **截圖之出處頁碼**（`> 📷 截自原始 PDF 第 X 頁`）與「判定個數／數值請以原始 PDF 核對」這類使用提醒。

分界是：註解談的是**原件說了什麼**（留），還是**我們怎麼把它變成 markdown**（刪）。

---

## 公式格式規範（Standing Instruction）

`statutes/`、`corpus/` 等所有 md 檔中的**數學公式一律使用 LaTeX 格式**：行內公式用 `$...$`，獨立公式用 `$$...$$`。

1. **禁止**以 ASCII 藝術（如 `┌──` 畫根號）、全形符號排版（如 `Ｗ＝－ln﹝－﹞`）或 code block 純文字近似呈現公式。
2. 轉寫或編修檔案時遇到既有非 LaTeX 公式，應一併轉為 LaTeX。
3. LaTeX 僅為呈現格式，公式**內容**仍以官方 PDF／原卷為準，轉換後須與原始檔核對。
4. **百分號 `%` 不得出現在數學模式（`$...$`／`$$...$$`）之內**。`%` 在 TeX 是註解符號，會把該行後面的內容整個吃掉；即使寫成跳脫的 `\%`，GitHub 的 markdown → math 管線也不保留那個反斜線，送進 KaTeX 的仍是裸的 `%`，結果就是公式只渲染到 `%` 之前（實例見 #94：`$$\text{偏差值}\,(\%) = …$$` 只顯示出「偏差值(」）。**作法**：公式維持 `$$...$$`，把 `(%)` 移到**該量首次被提及之處**的一般文字裡，例如「依下列公式計算變形比，其值用無條件捨去法取至個位。」改為「依下列公式計算變形比**(%)**，其值用無條件捨去法取至個位。」。行內數值之容差（如 `±3%`）則整段移出數學模式，寫成 `$f_1 = 1{,}056\,\text{Hz}$ ±3%`。若百分號夾在分數內部之中文標籤中而無法移出（如「相對濕度80%時之重量」），改以符號表示該量（`W_{80}`、`W_{60}`）並於公式下方以一般文字說明其定義。
5. **同一行（同一段落文字節點）內至多只放一段 inline math**。GitHub 的 markdown → math 管線在一行有多段 `$...$` 時只渲染第一段，其餘會原樣顯示成 `$W_{60}$` 這種原始碼（實例見 #94：「其中 $W_{80}$、$W_{60}$ 分別為…」只有第一個 `W_{80}` 呈現得出來）。**作法**：公式下方的變數說明一律改採**條列式**，每個符號各自成行——
    ```markdown
    公式中：

    - $L$：額定馬力（kW）
    - $Q$：額定出水量（m³/min）
    ```
    不得寫成「公式中 $L$：額定馬力（kW）； $Q$：額定出水量（m³/min）。」。若某一項的**說明文字裡又引用到另一個符號**（如「$N$： $H_n$ 數」），把被引用者改寫成一般文字或下標字元（「$N$：各配管管徑摩擦損失水頭之項數」、「W₈₀」），不要在同一行再開第二段 math。表格儲存格各自為獨立節點，不受此限，維持原狀即可；圖片 alt text 內不會渲染數學，一律改寫為一般文字。
6. **`\,` 是 LaTeX 的窄空格指令，不是逗號**（如 `0.4\,(\text{m}^2)` 渲染為「0.4 (m²)」，數值與單位間僅有一段細微間距）。本庫以之分隔數值與單位、或撐開括號內外，屬**刻意的排版寫法，保留不動**。讀 md 原始碼時它形似多餘的逗號，**校對時勿誤判為 OCR 誤植而移除**；反之，若原件該處確實有逗號，應寫成一般的 `,` 或 `，`，不得用 `\,` 代替。

---

## 上標符號規範（Standing Instruction）

`statutes/`、`corpus/` 等所有 md 檔中，**任何有平方的數字／單位一律以上標 `²` 表示**（如 `cm²`、`m²`、`mm²`、`kgf/cm²`），**禁止**寫成 `cm2`、`m2` 等。立方同理用 `³`。轉寫或編修檔案時遇到既有 `cm2` 等寫法，應一併改正。

---

## 編號段落分行規範（Standing Instruction）

法規條文之階層編號（`一、`／`（一）`／`１、`／`（１）`／`A.`／`(A)`／`a.`／`(a)` 等）**每一編號項須獨立成行**，不得多個編號項黏連於同一行（OCR／PDF 轉換常見）。轉寫或編修時遇黏連段落應拆行，拆行時注意：

1. 編號字母／數字**依序遞增**才視為新項目起點；順序不符者多為引用，不拆。
2. 「如（Ａ）」「依前（Ｃ）」「準用…」等**引用**不得拆行；規格標示（如「保險絲（Ａ）」之安培）亦非編號。
3. 句號式編號（`a.`）須句點後接中文內容才視為編號，避免誤拆小數（`2.5`）與英文縮寫。

---

## 編號括號全形規範（Standing Instruction）

`statutes/`、`corpus/` 等所有 md 檔中，條文階層編號之括號**一律使用全形 `（）`**（如 `（一）`、`（１）`、`（Ａ）`、`（ａ）`），**禁止**使用半形 `(一)`、`(1)` 等（OCR／轉換常見殘留）。此規範適用於內文與表格儲存格。轉寫或編修檔案時遇既有半形編號括號，應一併改正；修正後以全域搜尋確認無殘留。（包夾一般文字之括號亦以全形為原則，惟英數規格標示如 `D13（或 ψ13mm）` 之外層括號改全形、內部英數維持半形。）

---

## 同法規格式一致性（Standing Instruction）

同一部法規（含逐章拆檔者之各章檔與 hub 檔）之格式為一體：**凡對其中任一章／任一段落做了格式上的改動**（如拆行、LaTeX 公式、`²` 上標、表格結構化、附圖截圖嵌入、檔首說明調整等），**必須一併檢視該法規之其他所有章節**，將同類情形全部套用相同處理，務求整部法規格式一致，不得只改被指出的那一處。修正後應以全域搜尋（grep 等）確認無同類殘留。

---

## 檢查表／附圖截圖嵌入原則（Standing Instruction）

法規（尤其檢修基準類）之**章末檢修報告表／檢查表不辨識文字**，一律將該表所在之原始 PDF 頁面截圖為 PNG，以 Markdown 圖片連結嵌入；**內文附圖與表格內之圖示**亦同樣截圖後以連結嵌入，不以文字近似或省略。

1. 圖檔與引用之 md 檔放在**同一個資料夾**，檔名前綴與該 md 檔相同（如 `第04章_自動撒水設備_檢查表_p1.png`、`第18章_避難器具_圖18-1.png`）。
2. md 中於原表格／圖說位置以 `![說明](檔名.png)` 嵌入，並註明對應原始 PDF 頁碼與原始 PDF 連結。
3. 表格（編號附表）若為純文字內容仍依「附表處理原則」轉為結構化 Markdown；含圖之儲存格則將圖示個別截圖嵌入該列。

---

## statutes 檔名規則

法規 md 依命題大綱章節順序編號，格式 `<節>_<序2位>_<法規名>.md`（序號補零至 2 位以利排序），例：`1_01_消防法.md`、`2_01_各類場所消防安全設備設置標準.md`、`3_01_建築法.md`。第四節「認可基準」置於 `statutes/4_認可基準/` 子資料夾（以節號 `4_` 為前綴），格式 `4_<序2位>_<法規名>.md`。新增法規時依此規則命名並更新 `statutes/index.md`。

### 資產（附圖／逐章拆檔）資料夾規則

`statutes/` 下**每個裝載某支法規資產的子資料夾，一律以該法規主檔編號為前綴、名稱與主檔同名（去 `.md`）**，使資料夾與主檔並排對齊：

1. **有附圖之法規** → 圖檔置於 `statutes/<編號_法規名>/`（例：`1_12_防火牆及防火水幕設置基準/附圖一_防護位置.png`）。根目錄只保留法規 md 與 `index.md`，不平鋪圖檔。
2. **逐章拆檔之法規**（如 `2_03`、`2_10`）→ 各章 md 及其圖檔置於 `statutes/<編號_法規名>/`（例：`2_03_消防安全設備及必要檢修項目檢修基準/第01章_滅火器.md`），hub 主檔以章目錄表連結各章。
3. **資料夾內圖檔檔名去除冗餘法規前綴**（資料夾已提供命名空間），逐章拆檔者則以章為前綴（如 `第01章_滅火器_圖1-1.png`）。
4. **分類容器**：`statutes/4_認可基準/`（第四節分類，內含 `4_01`～`4_12` 多支法規）以節號 `4_` 為前綴；`statutes/原始檔案/`（各法規原始來源檔）為唯一不加編號前綴之例外。
5. **附檔名一律小寫**（`.pdf`／`.jpg`／`.png`／`.doc`／`.odt`）。

---

## 版本號規範（Standing Instruction）

**只要開 PR 就必須新增版本號。** 每次建立 pull request 前，一律先升 plugin 版本號，且需**兩處同步**：

1. `.claude-plugin/plugin.json` 之 `"version"`。
2. `README.md` 檔首的 `![Version]` 徽章。

版本遞增依語意化版本（SemVer，`MAJOR.MINOR.PATCH`）：新增功能升 MINOR、僅修正／文件升 PATCH、破壞性變更升 MAJOR。並於根目錄 `CHANGELOG.md` 之「版本紀錄」補一則對應條目（`## [x.y.z] - YYYY-MM-DD — 一句話標題`，最新在上；該檔已無目錄區塊，不需補目錄連結）。

> 兩處版本是否一致由 CI（`scripts/ci_check_repo.py` 之 `manifests` 檢查）把關，不一致會讓 PR 轉紅。
> 未隨版本發布的資料整理工作（未升版者）改記於 CHANGELOG.md 的「未隨版本發布的資料與內容工作」節。
