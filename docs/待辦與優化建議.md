# 待辦與優化建議

> 本文件僅保留**尚未完成**之改善提案與內容缺口。已完成之歷史批次清單（law2–6 入庫核對、認可基準截圖更正、檔名編號、免責聲明等）已於 2026-06-19 整理時移除；完成里程碑見文末。

---

## 一、題目存取效能：`corpus/questions.json` 平坦索引（提案，未實作）

現況存取分兩步：`tags_index.json` 取得參照（O(1)）→ 逐行掃描該 md 找 `### 第 N 題`（O(k)）。

**改善**：新增 `scripts/build_questions_index.py`（於 `build_tags.py` 後執行），產出 `corpus/questions.json`，每題存 `{ref, type, stem, options, answer, tags, md}`，skill 以 `questions_index[ref]` 達到真正 O(1)。

注意：markdown 為唯一真相來源；`questions.json` 為衍生產物，應列入 `.gitignore` 或標註 auto-generated，md 變更後須重建。

## 二、標籤分類校正（以語意判斷為準，非關鍵字 script）

**方針（2026-06-19 起）**：分類準確度以**語意判斷**為準，不再以 `build_tags.py` 關鍵字結果為最終值。`build_tags.py` 僅作初稿與「題型／旗標／條號」等客觀維度之來源；**系統／設備／法規／知識領域以各題 inline `🏷️` 標籤為唯一真相來源**。校正流程：直接編輯該題 `🏷️` 行 → 執行 `scripts/rebuild_index.py`（解析 inline 標籤反建 `tags_index.json`，不重判題目）。

**已套用之語意修正**：
- **緊急廣播設備 → 避難逃生設備（避難系統）**：全庫 73 題之「系統」維度已由警報改為避難（設備名不變）。警報設備 530→479、避難逃生設備 248→315。

**仍待語意校正（使用者人工核對中，本項保留）**：
- 跨系統題：含多設備者之系統歸屬是否齊全。
- by_law 漏標：引用多法條之申論題可能僅標到部分。
- 跨科題：法規卷中含設備計算者，可能缺設備標籤。
- 設備過度標記：關鍵字誤命中（如題幹僅順帶提及）者應移除。

校正優先序：by_equipment → by_system → by_law。

## 三、Skills 整合查詢入口（by_article / questions.json）

`exam-tutor`、`exam-trend-forecast` 尚未明確以 `tags_index.json`／未來 `questions.json` 為查詢入口。建議於各 SKILL.md 載明查詢流程，並善用新增的 **`by_article`** 維度支援「依單一條文複習歷年題」，免逐一掃描 `corpus/md/**`。

## 四、題目內容品質校核

| 項目 | 說明 |
|------|------|
| 圖形題（`has_figure`） | 共 4 題，選項僅佔位說明，無法純從 md 作答 |
| 更正答案（`更正答案` flag） | 共 14 題，已標 `#`，建議二次核對 |
| 兩欄排版選項順序 | pdftotext 偶造成 A/B/C/D 順序錯誤（已知修正士101/0907 Q40）；其餘年份未全數逐題比對 |

## 五、`tags_index.json` 載入優化（提案）

目前展開所有參照陣列；若 skill context 有限，可改存 `{ref: [tag,...]}` 倒排索引，由 skill runtime 反查。

## 六、年度難易度與命題頻率分析（提案）

以 `tags_index.json` 計算各設備歷年出題頻率、計算題佔比趨勢、找錯誤 vs 找正確比例變化等，對 exam-trend-forecast 有直接價值。

## 七、statutes 尚待補充之內容缺口

> 各法規檔首均已附「📌 免責聲明」，引用前以官方公告現行版為準。下列為仍可補強處：

- **公式對照官方 PDF**：各類場所設置標準 §83-2（惰性氣體藥劑量）、§97-3（鹵化烴藥劑量）、§133（緊急廣播音壓）之數學排版尚未經單條 PDF 核對。
- **缺原始檔待補**：
  - 設置標準 §84（二氧化碳放射時間表，RTF 遺失、未附 PDF）。
  - 消防機關辦理建築物消防安全設備審查及查驗作業基準：附件二、四、七、八、十一未提供。
  - 緊急電源容量計算基準：附錄一～六、附表 1～4、圖 1～4 未取得。
  - 原有合法建築物公共安全改善辦法：附表一、二未提供。
- **版本日期待補**：水道連結型自動撒水設備設置基準、測試報告書各章（原檔未標）。
- **圖例（未轉換，僅連結）**：設置標準 §164（救助袋角度圖）／§183／§191、消防幫浦性能/效率曲線圖、緊急電源圖 1～4 等。

## 八、雜項（低優先）

- 撒水頭 md 為 `4_08_撒水頭認可基準.md`，附件資料夾為 `密閉式撒水頭認可基準/`（md 內連結正確，僅名稱不一致）。

---

## 完成里程碑（記錄）

- **2026-06-19**：命題大綱所列法規（index.md 第一～四節）全數入庫並編號（`<節>_<序2位>_名`，認可基準置於 `statutes/認可基準/`）；3360 題完成標籤（含新增 `by_article` 條號維度）；核心法律「上傳版 vs 記憶版」比對（見 `docs/比對_上傳版vs記憶版.md`）；認可基準依使用者截圖逐項核對更正；所有法規檔首加「📌 免責聲明」；移除殘留空目錄與暫存進度檔。

---

*最後更新：2026-06-19*
