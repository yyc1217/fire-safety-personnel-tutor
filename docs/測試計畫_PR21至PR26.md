# 測試計畫：PR #21 至 PR #26 修改驗證（v0.6.1 → v0.8.0）

> 對應 issue #27。承接 issue #14（v0.6.0/0.6.1 全功能驗證）之後的修改：**PR #20/#21（猜題兩段式）、#22（體檢 v0.6.2）、#23（docs 範例）、#24（輸出格式範本 v0.7.0）、#25（週期統計按科目獨立）、#26（`/掌握度` v0.8.0）**。issue #14 已驗證通過的功能**不重測**；僅測本批 PR 新增或改動的行為，另承接 issue #14 遺留 2 個未測項（§6）。
>
> 測試方式：在已安裝本 plugin（v0.8.0）的 Claude Code session 中，依各案例「觸發語」操作，對照「預期結果」逐項打勾。標「**(資料檢查)**」者可直接在 repo 內以命令驗證，毋需啟動 skill。所有互動產出應為**繁體中文**。
>
> 建議以「全新 `~/.fire-safety-tutor/`」與「已有設定/進度」兩種狀態各跑一輪；`/掌握度` 與批改寫入類案例需在「已有作答紀錄」狀態下驗證。

---

## 0. 資料完整性（資料檢查，可命令驗證）

- [ ] `plugin.json` `version` = `0.8.0`，與 README badge、`docs/變更紀錄.md` 最新一節一致 ← **已知未通過**（訂定本計畫時檢查：README badge 仍為 `0.7.0`，變更紀錄最新僅至 v0.7.0；PR #25、#26 均未補記）
- [ ] `reference/輸出格式/` 11 個格式範本齊全（出題呈現／弱點筆記／懶人包／批改輸出／掌握度圖表／擬答／猜題報告／記憶整理／試卷／讀書計畫／題庫查詢與複習），且全數登錄於 `reference/index.md`（PR #24/#26）
- [ ] `reference/火災學主題知識點索引.md` 存在，主題數與 `corpus/tags_summary.json` `by_topic`（19 主題）嚴格對齊（PR #26）
- [ ] `reference/法規條文清單索引.md` 存在；`python3 scripts/build_article_list.py` 可重跑且輸出與提交版一致（PR #26）
- [ ] `python3 scripts/analyze_cycles.py` 執行無誤；`corpus/tags_cycles.json` 為合法 JSON，結構為 `{等別:{科目:{維度:{tag}}}}` 各科分層（師 6 科／士 4 科）（PR #25）
- [ ] `tags_cycles.json` 型態分類含「退役常年型」；抽查：師避難科 §188、師消防法規科 消防法 §11 標為退役常年型（PR #25）
- [ ] `corpus/命題週期分析.md` 為「各等別 → 各科目」分節（PR #25）
- [ ] `skills/掌握度/SKILL.md` 存在且 `disable-model-invocation: true`；本批全部指令 skill 該欄位皆為 true（PR #26）
- [ ] `reference/試卷格式.md` 卷型敘述與 `corpus/index.json` 相符：**師僅消防法規為混合卷，火災學及四系統科為純申論**；士全部 4 科混合卷（PR #22 項 1）
- [ ] 殘留字樣 grep 覆核：skill 與 README 無「信心程度」欄位要求殘留、無「快範」、study-planner 範例為「師 404 題」非「842」（PR #22 項 3/4/8）
- [ ] `docs/範例_猜題報告.md`：檔首內容索引錨點連結有效；檔末六種週期型態範例考點與 `corpus/命題週期分析.md` 數據一致（PR #23）
- [ ] `reference/user-config-spec.md` 內嵌 JSON 範例為合法 JSON；`coverage` schema 含設備 `asked`/`done`/`next` 拆分、`by_article:`（含 `total`）、`by_topic:`（PR #26）

## 1. 猜題兩段式與親民用語（PR #20、#21、#22）

- [ ] 「今年可能考什麼」/`/猜題` → **先秒級產出第一段「統計結果」**（純本地計算），不先上網
- [ ] 第一段報告**第一層以考試科目分層**，各科分「常考重點／回鍋候選／冷門與缺口」，版面比照 `reference/猜題報告格式.md`
- [ ] 第一段產出後**中段詢問**是否上網補近 12–24 個月修法／函令／時事，並告知約需 5–8 分鐘（視設定與網路而定）
- [ ] 使用者同意 → 執行第二段「近期動態」；**拒絕 → 以第一段為最終輸出並誠實標注「未含近期動態」**
- [ ] 無網路時優雅退場：只出第一段統計結果並說明
- [ ] **親民用語**：對使用者輸出一律為「條文／設備／主題／法規／系統：可讀範圍」，**不露出 `by_article:` 等內部代碼**
- [ ] 報告**不列「信心程度」欄位**，考點強弱由依據措辭表達（PR #22 項 3）
- [ ] `/猜題` 指令行為與 `猜題報告格式.md` 一致：兩段式、科目分層，非舊版「四層動態報告」（PR #22 項 2）
- [ ] `level=士` 第一段額外列出**測驗題型高頻數字考點**（附數值，引對照表）（承 issue #14 §3 未測項＋PR #19 修正）

## 2. 週期統計按科目獨立（PR #25）

- [ ] 猜題依據**按科目（卷別）獨立判讀**：同一條文在不同科的週期分開陳述，無跨科、跨等別合併推論
- [ ] **退役常年型**（該科歷史常年但近 ≥2 屆缺席）正確呈現並列為高信心回鍋候選（例：師避難科 §188）
- [ ] **型態配題型**：頻率高但本卷非其常出題型的考點被降優先（例：師消防法規科 §11 幾乎全為選擇題，申論猜題不高掛）
- [ ] **同科叢集**：同科相鄰／相關條文同進同退時整群評估回鍋，且不跨科計算
- [ ] 「偶發」型考點列低優先，僅動態觸及才上調（PR #22 項 5）

## 3. 輸出格式範本落地（PR #24）

> 各案例驗證重點＝實際輸出**逐區塊比照**對應範本檔，非僅內容正確。

- [ ] **批改輸出**：出題作答後，申論／選擇題批改版面、法源標注格式、輪末成績摘要比照 `reference/輸出格式/批改輸出格式.md`
- [ ] **弱點筆記**（`weakness_tracking = "notes"`）：產出筆記欄位固定（考點 tag、日期、題目參照、錯誤描述），且 `/弱點複習` 能**讀回**筆記選題
- [ ] **懶人包**：`/懶人包` 產出比照 `懶人包格式.md`——檔首狀態列、考點分節、「⚠ 你的弱點」標注且排序優先、對照表引註，完成後詢問是否存入 `forecasts/`
- [ ] **擬答**：`/申論猜題` 產出三區塊（預測題目／答題架構／擬答）標題層級與版面比照 `擬答格式.md`，內容維持「要件→法源→涵攝→小結」
- [ ] **讀書計畫**：逐週排程形式比照 `讀書計畫格式.md` 範例；勾銷附日期；已有計畫檔啟動時之進度報告格式固定，且計畫檔可跨對話讀回解析
- [ ] **記憶整理**：記憶卡／口訣／易混淆組三種產出版面比照 `記憶整理格式.md`
- [ ] 即時生成對照表版面比照內建表；exam-archive 題庫清單表格欄位固定（比照 `題庫查詢與複習格式.md`）

## 4. `/掌握度` 視覺化（PR #26）

### 呈現（唯讀）

- [ ] `/掌握度`（或「熟悉度」「我對哪些比較弱」）→ 產出掌握度地圖，版面比照 `reference/輸出格式/掌握度圖表格式.md`；全程**不寫任何檔**
- [ ] **🔥 火災學**：19 主題全列（含 0%），20 格實心條 `[■…□] XX%`＋「覆蓋 已掌握／總點數」欄，高頻在前
- [ ] **📜 消防法規**：預設列前 5 大法規＋有紀錄之其他法規；每部**全條文**依條號每 10 條一列、8 級量化方塊（未接觸＝`▁`）；標題附整體 %（＝各條 % 之算術平均）
- [ ] **🧯 消防安全設備**：依水／化學／避難／警報四節，`by_equipment` 全部設備逐一 20 格實心條
- [ ] 尚未接觸項目一律以 **0%** 計且**全列**（完整地圖，不隱藏）
- [ ] 呈現時**零重掃 statutes**（分母取自索引檔與 `coverage` 既存 `total`）
- [ ] 檔首標注資料來源與統計期間，並註明「僅為本機作答統計，非官方能力認證」
- [ ] 無 `progress.json` 或無作答紀錄時優雅退場（說明並指路出題功能，不杜撰數據）

### 批改寫入（exam-tutor 端）

- [ ] 批改後 `coverage` 正確寫入：設備項 `asked`（已問過）與 `done`（判定已掌握）**分開記錄**，不混寫
- [ ] 法條項寫入 `by_article:<條號短名>` 之 `{done, total}`，`total`＝語意切分之獨立要點數（非項／款數）；**該條已有 `total` 時沿用既存切分，不重切**
- [ ] 火災學寫入 `by_topic:<主題>` 之 `done`，知識點名稱對齊 `reference/火災學主題知識點索引.md`
- [ ] 選擇題**答對**即計入該題所考內容點
- [ ] key 命名穩定：`by_article:` 對齊 `tags_index.json` 維度（增訂條文用「第 N 條之 M」；檢修基準以章為單位）
- [ ] **舊檔遷移**：讀到無 `asked` 欄的設備項時，`done` 整批改名為 `asked` 並寫回，`done` 由批改重新累積
- [ ] study-planner 只讀設備（bare-key）項之 `asked`/`next`，**忽略 `by_article:`／`by_topic:` 前綴 key**

## 5. 貫穿原則（僅驗新增輸出面）

- [ ] 本批全部新輸出（猜題兩段、掌握度地圖、各格式範本產出）皆為**繁體中文**、公式一律 LaTeX
- [ ] 新輸出之法源標注一致（法規名＋條號＋版本日期）；掌握度／猜題數據誠實標注來源與局限
- [ ] skill 邊界不越權：`/掌握度` 只呈現不出題（指路 exam-tutor）；`/猜題` 只猜題不出題

## 6. 承接 issue #14 遺留未測項

- [ ] `weakness_tracking = "none"` 時完全不寫任何檔（含 `coverage`——注意 PR #26 新增批改寫入路徑亦須遵守）、不再詢問是否記錄
- [ ] `level=士` 猜題數字考點分支（已併入 §1 最末項，士等別跑通即勾）

---

### 驗證環境

- plugin 版本：**v0.8.0**（main @ PR #26 合併後）
- 前置：`~/.fire-safety-tutor/` 需備妥「全新」與「已有 progress.json 作答紀錄」兩種狀態；§4 批改寫入類另需造「舊版 coverage（無 `asked` 欄）」檔驗證遷移

_本檔為測試計畫清單，發現的問題請另開 issue 並回連本清單對應項目。_
